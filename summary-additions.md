
# أقسام جديدة — 8 أغسطس 2026

> **طريقة الاستخدام:** الصق الأقسام دي في آخر `PROJECT-SUMMARY-AR.md`، وعدّل الأقسام المشار إليها في القسم 29.

---

## 26) 🛡️ حماية النشر من النسخ القديمة (اتنفذ — 8 أغسطس)

**المشكلة:** أداة النشر كانت بتجيب الملف من GitHub قبل كل رفعة عشان `sha` بس، وترمي المحتوى. فأي نسخة قديمة كانت بتتنشر فوق شغل أحدث، والأداة تقول «✓ Publisert» عادي.

### أ) النتايج الستة

| النتيجة | الشرط | التصرف |
|---|---|---|
| 🆕 `ny` | مش موجود في الريبو | ✅ اسمح |
| ✓ `nyere` | ختمك أحدث من المنشور | ✅ اسمح |
| ✓ `endret` | نفس الختم، محتوى مختلف | ✅ اسمح |
| ✓ `ukjentNy` | مفيش ختم، والمحتوى مش في التاريخ | ✅ اسمح |
| ⛔ `identisk` | نفس المحتوى بالظبط | 🛑 **منع** |
| 🛑 `eldre` | ختمك أقدم من المنشور | 🛑 **منع** |
| 🛑 `historisk` | المحتوى = نسخة قديمة في التاريخ | 🛑 **منع** |

📌 **`endret` هي أهم صف في الجدول.** لما تنزّل ملف وتعدّله، الختم بيفضل زي ما هو — نفس ختم النسخة المنشورة. لو اتحسبت «تضارب»، الأداة كانت هتقفل على **كل تعديل عادي** وتتحول لإزعاج يتجاهله.

### ب) تلات طبقات بالترتيب

1. **الختم** — لو النسخة راجعة من GitHub. فوري، بلا طلبات زيادة.
2. **السجل المحلي** (`localStorage`) — بصمة كل ملف اتنشر (آخر 60 لكل ملف). لو البصمة في السجل ومش الأحدث → منع فوري.
3. **تاريخ GitHub** — آخر 30 نسخة. لو المحتوى طابق نسخة قديمة → منع، بالتاريخ وعدد النسخ لورا.

📌 **الحتة اللي بتخلي الفحص فوري:** الأداة بتقارن آخر حاجة اتنشرت من السجل بالمنشور فعلًا — والمقارنة دي **مجانية** لأنها جابت الملف أصلًا. لو اتطابقوا، السجل بيبقى موثوق كامل، و«مش في السجل» تبقى معناها يقينًا «جديد». الطبقة التالتة مابتشتغلش.

**القياس على 20 رفعة:** الأولى 32 طلب، وكل اللي بعدها **طلب واحد**.

### ج) الأساس المفقود

الختم بيتحقن في النسخة الرايحة لـGitHub **بس**. الملف على الجهاز مابيتغيرش. فاللي بيعدّل نفس النسخة المحلية طول الوقت **عمر ملفه ما هيبقى فيه ختم**، والطبقة الأولى مالهاش لازمة عنده.

📌 **الحل الجذري (مطلوب، مش متنفذ):** الختم يتولّد **وقت التعديل** مش وقت النشر. الاسم `SIST-ENDRET` بيقول «آخر تعديل» وبيسجّل «وقت النشر» — والاتنين حاجتين مختلفتين. لو اتصلح، حالة `endret` كلها بتختفي والمنطق يبقى حالتين بس.

⚠️ بس ختم التعديل **بيان بيكتبه اللي عدّل**؛ ينفع يضيع أو الساعة تبقى غلط. فحص التاريخ **حقيقة على سيرفر GitHub**. الاتنين لازم يفضلوا: الختم أساس، التاريخ شبكة تحته.

### د) حاجات تانية في الأداة

- **📜 اللوج** — بيمشي على تاريخ الملف ويتأكد إن الترتيب متسق. أخضر = تمام. أحمر = **بيقولك عند أي نسخة الخلل بدأ**، بالرقم والتاريخ.
- **⬇️ تنزيل / 📋 نسخ** — النسخة المنشورة، أو أي نسخة قديمة من اللوج (للإنقاذ بعد رجوع للخلف).
- **📦 Last ned hele prosjektet** — كل ملفات الريبو + الملخص في ZIP واحد، لبدء محادثة جديدة. مولّد ZIP مكتوب من الصفر، بدون مكتبات.

⚠️ **تحذير في الرفع المتعدد:** المتصفح بيدي اسم الملف بس، والأداة بتستنتج المسار. `index.html` موجود في **3 أماكن** (الجذر · `påmelingsskjema/` · `påmelingsskjema/påmelingsskjema/`) والأداة بتختار **الجذر دايمًا**. رفع `index.html` بتاع مجلد فرعي بالاسم **بيدوس على الصفحة الرئيسية**. استخدم «Annen filsti» لدول.

⚠️ **الحد 30 نسخة** في فحص التاريخ. أقدم من كده بيعدّي كـ«جديد».

---

## 27) 🔴 الباگ اللي خصى نظام الأختام (اتكشف واتصلح — 8 أغسطس)

**أهم درس في المشروع كله. الباگ عاش 24 ساعة وهو غير مرئي تمامًا.**

### الأعراض
كل ملف HTML أو MD اتنشر من 7 أغسطس 23:14 طلع **بلا ختم**، وبسطر فاضي زيادة بعد `<!DOCTYPE html>` بيتراكم مع كل رفعة. الأداة كانت بتقول «✓ Publisert» في كل مرة.

### السبب
`stemplFil` بتشيل الختم القديم قبل ما تحط الجديد:

```js
innhold.replace(/<!--\s*SIST-ENDRET:[^>]*-->\s*\n?/g, '')
```

ولما الملف المنشور هو **الأداة نفسها**، الـregex بيلاقي القالب المكتوب جوه الكود:

```js
const merke = '';
```

يشوفه ختم قديم، **ويمسحه**. يفضل `const merke = '';`.

فرع `.js` نجا لأن regex الـHTML بيدوّر على `<!-- -->` بس. فرع الـYAML نجا لأن الـ`^` عايز بداية سطر.

### الإصلاح
القالب اتفصل لقطع، فالنص `<!-- SIST-ENDRET:` مابقاش موجود كسلسلة متصلة في الكود:

```js
const MERKE_START = '<' + '!--';
const MERKE_SLUTT = '--' + '>';
```

🔴 **متكتبهمش كسلسلة واحدة تاني.** فيه تعليق أحمر فوقهم في `publish-tool.html`.

### إصلاح تاني في نفس المكان
تنضيف السطور الفاضية اتكتب غلط أول مرة: `/^\s*<!DOCTYPE[^>]*>\s*\n?/` كان بيبلع السطر الفاضي **جوه** المطابقة، فالتنضيف كان بيشتغل على الجهة الغلط. الـregex بقى يمسك تاج `<!DOCTYPE>` لوحده.

### 📌 الدرس
**أي أداة بتعالج ملفات وبتنشر نفسها هي حالة خاصة.** لازم تتسأل: «لو الأداة دي اشتغلت على كودها هي، هتعمل إيه؟»

الباگ ده مكانش هيتكشف بالتجربة العادية — الأداة بتشتغل تمام على كل الملفات التانية. اتكشف بس لما اتفحص محتوى الملف المنشور نفسه.

**التأكيد:** الرفعة الأولى بتوصّل الإصلاح، والتانية هي اللي بيشتغل فيها (لأن الأداة المكسورة هي اللي نشرت المتصلحة). أول ختم صحيح: **8 أغسطس 15:22:39**.

---

## 28) ✅ App Check — اتحل (8 أغسطس)

**كان 0% توكنات صالحة من 4 أغسطس. السبب كان سطر واحد.**

### السبب الجذري
مفتاح reCAPTCHA (`6LfsK2wt...`) كان مسجّل عليه دومين واحد:

```
madina.no
```

والموقع على **`madinaskole.no`**. دومين مختلف تمامًا — مش حتى GitHub Pages القديم زي ما كان متوقع. `madina.no` هو الدومين الأصلي للمشروع (لسه `kunngjoring.html` بيجيب اللوجو منه).

فكل طلب توكن كان بيترفض عند Google قبل ما يوصل Firebase. عداد reCAPTCHA كان **Total requests = 0**.

### الإصلاح
`google.com/recaptcha/admin` → المفتاح → ⚙️ Settings → Domains → ضيف:
```
madinaskole.no
madinaskolen.no
```

النتيجة: **✅ TOKENET BLE UTSTEDT — 952 حرف.**

### الحالة قبل الإصلاح (Aug 1–9، مرجع للمقارنة)
| النوع | النسبة | العدد |
|---|---|---|
| Verified | **0%** | 0 / 758 |
| Invalid | 93% | 705 |
| Outdated client | 4% | 29 |
| Unknown origin | 3% | 24 |

الـ93% دي كانت أثر الدومين الغلط مباشرة.

### 🔴 لسه ممنوع تفعيل Enforced
- الرقم متوسط على 7 أيام — محتاج **أسبوع** لحد ما الأيام المكسورة تخرج من النافذة. استخدم نطاق **24 ساعة** للمتابعة.
- الـ7% (Outdated + Unknown origin) **مش هيختفوا**: متصفحات بتحجب reCAPTCHA وأجهزة قديمة. Safari بـ«الحماية المتقدمة» منهم.
- **لو اتفعّل الفرض، الناس دي بتتقفل بره — وفيهم أهالي حقيقيين.**
- الشرط: فوق 90% ثابت على كام يوم.

### 🧪 `appcheck-test.html` — صفحة تشخيص جديدة
بتفصل المشكلة لـ3 خطوات (تحميل reCAPTCHA · الإعداد · طلب التوكن) وبتوقف عند أول فشل مع كود الخطأ وسببه بالنص.

| الكود | السبب |
|---|---|
| `recaptcha-error` | الدومين مش مسجّل · أو المفتاح Enterprise والكود v3 · أو مفتاح مشروع تاني |
| `fetch-status-error` | المفتاح سليم بس مش مربوط في Firebase App Check (ناقص الـsecret) |
| `throttled` | نتيجة الفشل المتكرر، مش السبب |

📌 بتستخدم **نفس** `firebaseConfig` ومفتاح reCAPTCHA بتوع الـ8 صفحات الحقيقية. لو اتغيروا هناك، لازم تتغير هنا كمان وإلا بتختبر حاجة تانية.

### حالة Firebase Console (مؤكدة 8 أغسطس)
- `Madina-Web` → ✅ Registered · reCAPTCHA مفعّل
- Firestore + Authentication → **Monitoring** (مش Enforced)
- ⚠️ **Functions مش محمية بـApp Check خالص** — تحتاج تفعيل من جوه الكود
- ⚠️ متدوسش «reCAPTCHA Enterprise» في الكونسول — الكود بيستخدم `ReCaptchaV3Provider`

---

## 29) 📝 تعديلات على أقسام قديمة

### القسم 5 (App Check)
**اتحل.** شوف القسم 28. سيب النص القديم كمرجع تاريخي أو استبدله.

### القسم 2 (جدول آخر تعديل)
| الملف | الحالة الجديدة |
|---|---|
| `foreldreinnsyn.html` | ✅ **اتصلح** 8 أغسطس — مابقاش مكسور |
| `larerinnsyn.html` | ✅ **اتصلح** 8 أغسطس — مابقاش مكسور |
| `publish-tool.html` | 8 أغسطس — حماية النشر + اللوج + التنزيل/النسخ + ZIP |
| `appcheck-test.html` | 🆕 ملف جديد |

⚠️ الجدول ده بقى للمرجعية بس — الختم الحقيقي جوه كل ملف، **ومن 8 أغسطس 15:22 بقى شغال فعلًا** لأول مرة.

### القسم 6 (عطل Safari)
`foreldreinnsyn` و `larerinnsyn` اتصلحوا: `initializeAppCheck` في `try/catch` · كل `onSnapshot` عدّت على `watch()` · `browserSessionPersistence` بدل `inMemoryPersistence` · زرار خروج.

🔴 **وعطل رابع مكنش في الملخص:** الصفحتين مكتوب فوقهم «kun for eier» بس الكود كان بيقبل **أي حساب في Firebase Auth**. يعني أي معلم يشوف نشاط كل زمايله، وأي ولي أمر يشوف كل الأهالي. اتحل بـ`TILLATTE_EPOSTER`.

⚠️ التحقق ده **في المتصفح** — بيمنع سوء الاستخدام مش المهاجم. الحماية الحقيقية لسه محتاجة `firestore.rules`.

### القسم 16 (المتبقي قبل الإطلاق)
**خلص:** ~~reCAPTCHA/App Check~~ · ~~`foreldreinnsyn` و `larerinnsyn`~~

**لسه:**
1. 🔴 رقم حساب الفواتير (`INVOICE_BANK_PLACEHOLDER` لسه `1234.56.78900`)
2. 🔴 تناقض نص الخصوصية
3. 🔴 حسم نسب خصم الإخوة
4. 🟡 `firestore.rules` + `programs` و `schoolCalendar`
5. 🟡 القسم 24 (مجموعة `settings`)
6. 🟡 ختم وقت التعديل (القسم 26-ج)
7. 🟡 App Check للـFunctions
8. 🟢 امسح `påmelingsskjema/påmelingsskjema/` (ملف 1 بايت، ومجلد باسم فيه `å`)
9. 🟢 امسح `index-homepage-final.html`

### القسم 18 (ملاحظات دائمة) — أضف
9. **أي أداة بتعالج ملفات وبتنشر نفسها لازم تتسأل: هتعمل إيه لو اشتغلت على كودها هي؟** (القسم 27)
10. **صناديق الحالة تتظهر بالـclass مش بـ`style.display`.** خلط الاتنين بيخلي `display:none` الـinline يغلب `.status.show` — الصندوق بيتملّي نص ويفضل مخفي. حصل فعلًا.
11. **الملف الكبير مايتكتبش من الأول.** النسخة في الريبو ممكن تكون أحدث من اللي معاك.

---

## 30) 🗓️ اللي حصل يوم 8 أغسطس

| # | الحدث | النتيجة |
|---|---|---|
| 1 | `foreldreinnsyn` + `larerinnsyn` مكسورين | ✅ اتصلحوا واتنشروا |
| 2 | الصفحتين بيقبلوا أي حساب | ✅ اتقفلوا على المالك |
| 3 | حماية النشر من النسخ القديمة | ✅ 3 طبقات |
| 4 | لوج التعديلات + تنزيل/نسخ + ZIP | ✅ اتعملوا |
| 5 | أداة النشر بتخصي نفسها | ✅ اتكشف واتصلح |
| 6 | App Check 0% | ✅ **اتحل** — الدومين |
| 7 | `appcheck-test.html` | ✅ صفحة تشخيص جديدة |

**دروس اليوم:**
- **الدليل من الجهاز حسم كل حاجة.** تشخيص App Check اتحدد من رسالة حمرا صغيرة في ركن الشاشة، مش من تحليل الكود.
- **الأداة اللي بتنشر نفسها حالة خاصة** — الباگ عاش 24 ساعة وهو غير مرئي.
- **فحص الـsyntax مسك ملفين مكسورين في يوم واحد.** الإضافات ابتلعت سطر تعريف دالة مرتين.
- **متكتبش ملف كامل وإنت مش شايف آخر نسخة منه.**
