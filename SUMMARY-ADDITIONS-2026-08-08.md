
# أقسام جديدة — 8 أغسطس 2026

> **الملف ده بيحل محل `summary-additions.md` القديم بتاع النهاردة الصبح.** الصق الأقسام دي في آخر `PROJECT-SUMMARY-AR.md`، وعدّل الأقسام المشار إليها في القسم 31.

---

## 🔴 اقرأ ده الأول — قيدان دائمان اتكشفوا النهاردة

| # | القيد | النتيجة |
|---|---|---|
| 1 | **القاعدة في `europe-north2`** | دوال Firestore (`onDocumentCreated` وأخواتها) **مستحيل تتنشر في المشروع ده**. المنطقة مش بتدعم Cloud Functions، ومنطقة القاعدة مش قابلة للتغيير بعد الإنشاء. البديل الوحيد: `onSchedule`. |
| 2 | **قالب الختم متجمّع من قطع** | `MERKE_START = '<' + '!--'` في `publish-tool.html`. لو اتكتب كسلسلة واحدة، الأداة بتمسحه من كودها هي لما تنشر نفسها. |

⚠️ **القيد الأول كلّف ساعتين النهاردة.** الكود كان سليم، والصلاحيات سليمة، والخدمات مفعّلة — والمشكلة كانت منطقة القاعدة. أي محادثة جاية تكتب `onDocumentCreated` هتضيّع نفس الوقت.

---

## 26) 🛡️ حماية النشر من النسخ القديمة (اتنفذ)

**المشكلة:** أداة النشر كانت بتجيب الملف من GitHub قبل كل رفعة عشان `sha` بس، وترمي المحتوى. فأي نسخة قديمة كانت بتتنشر فوق شغل أحدث، والأداة تقول «✓ Publisert» عادي.

### أ) النتايج السبعة

| النتيجة | الشرط | التصرف |
|---|---|---|
| 🆕 `ny` | مش موجود في الريبو | ✅ اسمح |
| ✓ `nyere` | ختمك أحدث من المنشور | ✅ اسمح |
| ✓ `endret` | نفس الختم، محتوى مختلف | ✅ اسمح |
| ✓ `ukjentNy` | مفيش ختم، والمحتوى مش في التاريخ | ✅ اسمح |
| ⛔ `identisk` | نفس المحتوى بالظبط | 🛑 **منع** |
| 🛑 `eldre` | ختمك أقدم من المنشور | 🛑 **منع** |
| 🛑 `historisk` | المحتوى = نسخة قديمة في التاريخ | 🛑 **منع** |

📌 **`endret` أهم صف في الجدول.** لما تنزّل ملف وتعدّله، الختم بيفضل زي ما هو. لو اتحسبت «تضارب»، الأداة كانت هتقفل على **كل تعديل عادي** وتتحول لإزعاج يتجاهله.

### ب) تلات طبقات بالترتيب

1. **الختم** — لو النسخة راجعة من GitHub. فوري، بلا طلبات زيادة.
2. **السجل المحلي** (`localStorage`) — بصمة كل ملف اتنشر (آخر 60 لكل ملف).
3. **تاريخ GitHub** — آخر 30 نسخة. المحتوى طابق نسخة قديمة → منع، بالتاريخ وعدد النسخ لورا.

📌 **الحتة اللي بتخلي الفحص فوري:** الأداة بتقارن آخر حاجة اتنشرت من السجل بالمنشور فعلًا — والمقارنة دي **مجانية** لأنها جابت الملف أصلًا. لو اتطابقوا، السجل بيبقى موثوق كامل، والطبقة التالتة مابتشتغلش.

**القياس على 20 رفعة:** الأولى 32 طلب، وكل اللي بعدها **طلب واحد**.

### ج) الأساس المفقود

الختم بيتحقن في النسخة الرايحة لـGitHub **بس**. الملف على الجهاز مابيتغيرش. فاللي بيعدّل نفس النسخة المحلية طول الوقت **عمر ملفه ما هيبقى فيه ختم**.

📌 **الحل الجذري (مطلوب، مش متنفذ):** الختم يتولّد **وقت التعديل** مش وقت النشر. الاسم `SIST-ENDRET` بيقول «آخر تعديل» وبيسجّل «وقت النشر» — والاتنين حاجتين مختلفتين.

⚠️ بس ختم التعديل **بيان بيكتبه اللي عدّل**. فحص التاريخ **حقيقة على سيرفر GitHub**. الاتنين لازم يفضلوا: الختم أساس، التاريخ شبكة تحته.

### د) حاجات تانية في الأداة

- **📜 اللوج** — بيمشي على تاريخ الملف ويتأكد إن الترتيب متسق. أحمر = **بيقولك عند أي نسخة الخلل بدأ**.
- **⬇️ تنزيل / 📋 نسخ** — النسخة المنشورة أو أي نسخة قديمة (للإنقاذ بعد رجوع للخلف).
- **📦 Last ned hele prosjektet** — كل الملفات + الملخص في ZIP. مولّد ZIP مكتوب من الصفر بدون مكتبات.

⚠️ **الأرشيف بيستبعد الملفات المخفية** — يعني `.env.madina-instituttet` و `.firebaserc` **مش جواه**. لو هتنشر الدوال من الجهاز، لازم تعملهم بإيدك (شوف القسم 30-د).

⚠️ **`index.html` موجود في 3 أماكن** (الجذر · `påmelingsskjema/` · `påmelingsskjema/påmelingsskjema/`) والأداة بتختار **الجذر دايمًا**. رفع `index.html` بتاع مجلد فرعي بالاسم **بيدوس على الصفحة الرئيسية**.

---

## 27) 🔴 الباگ اللي خصى نظام الأختام

**الباگ عاش 24 ساعة وهو غير مرئي تمامًا.**

### الأعراض
كل ملف HTML أو MD اتنشر من 7 أغسطس 23:14 طلع **بلا ختم**، وبسطر فاضي زيادة بعد `<!DOCTYPE html>` بيتراكم مع كل رفعة. الأداة كانت بتقول «✓ Publisert» في كل مرة.

### السبب
`stemplFil` بتشيل الختم القديم قبل ما تحط الجديد:

```js
innhold.replace(/<!--\s*SIST-ENDRET:[^>]*-->\s*\n?/g, '')
```

ولما الملف المنشور هو **الأداة نفسها**، الـregex بيلاقي القالب المكتوب جوه الكود، يشوفه ختم قديم، **ويمسحه**. يفضل `const merke = '';`.

فرع `.js` نجا لأن regex الـHTML بيدوّر على `<!-- -->` بس. فرع الـYAML نجا لأن الـ`^` عايز بداية سطر.

### الإصلاح
```js
const MERKE_START = '<' + '!--';
const MERKE_SLUTT = '--' + '>';
```

🔴 **متكتبهمش كسلسلة واحدة تاني.**

### 📌 الدرس
**أي أداة بتعالج ملفات وبتنشر نفسها هي حالة خاصة.** لازم تتسأل: «لو الأداة دي اشتغلت على كودها هي، هتعمل إيه؟» الباگ ده مكانش هيتكشف بالتجربة العادية — الأداة بتشتغل تمام على كل الملفات التانية.

**أول ختم صحيح: 8 أغسطس 15:22:39.**

---

## 28) ✅ App Check — اتحل

**كان 0% توكنات صالحة من 4 أغسطس. السبب كان سطر واحد.**

### السبب الجذري
مفتاح reCAPTCHA (`6LfsK2wt...`) كان مسجّل عليه دومين واحد: **`madina.no`** — والموقع على **`madinaskole.no`**. دومين مختلف تمامًا، مش حتى GitHub Pages القديم زي ما كان متوقع.

`madina.no` هو الدومين الأصلي للمشروع (لسه `kunngjoring.html` بيجيب اللوجو منه).

### الإصلاح
`google.com/recaptcha/admin` → المفتاح → ⚙️ Settings → Domains → ضيف `madinaskole.no` و `madinaskolen.no`.

النتيجة: **✅ TOKENET BLE UTSTEDT — 952 حرف.**

### الحالة قبل الإصلاح (Aug 1–9، مرجع للمقارنة)
| النوع | النسبة | العدد |
|---|---|---|
| Verified | **0%** | 0 / 758 |
| Invalid | 93% | 705 |
| Outdated client | 4% | 29 |
| Unknown origin | 3% | 24 |

### 🔴 لسه ممنوع تفعيل Enforced
- الرقم متوسط على 7 أيام — استخدم نطاق **24 ساعة** للمتابعة.
- الـ7% (Outdated + Unknown origin) **مش هيختفوا**: متصفحات بتحجب reCAPTCHA وأجهزة قديمة.
- **لو اتفعّل الفرض، الناس دي بتتقفل بره — وفيهم أهالي حقيقيين.**
- الشرط: فوق 90% ثابت على كام يوم.

### 🧪 `appcheck-test.html` — صفحة تشخيص جديدة
بتفصل المشكلة لـ3 خطوات (تحميل reCAPTCHA · الإعداد · طلب التوكن) وبتوقف عند أول فشل مع كود الخطأ وسببه بالنص.

| الكود | السبب |
|---|---|
| `recaptcha-error` | الدومين مش مسجّل · أو المفتاح Enterprise والكود v3 · أو مفتاح مشروع تاني |
| `fetch-status-error` | المفتاح سليم بس مش مربوط في Firebase App Check |
| `throttled` | نتيجة الفشل المتكرر، مش السبب |

📌 بتستخدم **نفس** `firebaseConfig` ومفتاح reCAPTCHA بتوع الصفحات الحقيقية. لو اتغيروا هناك، لازم تتغير هنا كمان.

### حالة Firebase Console (مؤكدة 8 أغسطس)
- `Madina-Web` → ✅ Registered · reCAPTCHA مفعّل
- Firestore + Authentication → **Monitoring** (مش Enforced)
- ⚠️ **Functions مش محمية بـApp Check خالص**
- ⚠️ متدوسش «reCAPTCHA Enterprise» — الكود بيستخدم `ReCaptchaV3Provider`

---

## 29) ⚙️ مجموعة `settings` — اتنفذت جزئيًا

القسم 24 كان مقترح. دي أول مرحلة منه، **متنفذة وشغالة**.

### أ) `settings/priser`

```
soskenRabatt2: 20        // نسبة مئوية
soskenRabatt3: 30
tellForesatt2: false
```

**بتتعدّل من:** اللوحة → Administrativ → Betalingsinnstillinger → كارت **Søskenrabatt**، مع معاينة حية بالكرونة.

### 🔴 العطل الحي اللي اتصلح

`admin.html` كان بقى بيقرا السعر من مجموعة `programs` (تبويب Programmer)، و`functions/index.js` لسه بيقرا من ثوابت مكتوبة جواه.

**يعني تغيير سعر من اللوحة كان بيخلي اللوحة تحسب رقم وفيبس يخصم رقم تاني.** دلوقتي الاتنين بيقروا من نفس المكان — الأسعار من `programs`، والخصم من `settings/priser`، والثوابت بقت احتياطي لو القاعدة ما ردّتش.

⚠️ **`tellForesatt2` قيمته `false`** — يعني السلوك ما اتغيرش. الطفل المسجّل تحت ولي الأمر التاني **مش بيتحسب كأخ والعيلة بتدفع أكتر** (القسم 12). المفتاح موجود عشان يتصلح بقرار، مش لوحده.

### ب) `settings/varsler`

```
nySoknadAktiv: true
kvitteringAktiv: true
nySoknadMottakere: ["..."]   // إيميلات، مش IDs
varselInitiert: true          // حارس أول تشغيلة — متلمسهوش
```

📌 **المستقبلين محفوظين كإيميلات مش IDs** — عشان الإشعار يفضل شغال حتى لو الكارت الشخصي اتمسح واتعمل تاني. وفيه خانة إدخال يدوي لأي عنوان مش مربوط بموظف.

### ج) القيم اللي لسه في الكود

| القيمة | فين |
|---|---|
| `INACTIVITY_LIMIT_MS` (20 دقيقة) | `admin.html` + `index1.html` |
| أجر الساعة (200 معلم / 150 مساعد) | `admin.html` |
| `AUTO_CLOSE_DAYS` (14) | `index1.html` |
| `CONFIRM_LOCK_DAYS` (30) | `slettelogg.html` |
| `MAX_FILE_MB` (10) | 3 ملفات |
| `KEEP_DAYS` (60) | `functions/index.js` |
| `INVOICE_BANK_PLACEHOLDER` | `admin.html` |

### د) 🔴 اللي **لازم يفضل** في الكود

**الأدوار والصلاحيات** (`OPPTAK_ROLLER` · `ADMIN_PANEL_ROLES`). القواعد الحالية بتسمح لأي حساب مسجّل يكتب في أي مجموعة — لو الأدوار اتنقلت للقاعدة، **أي معلم يقدر يدّي نفسه دور Eier**.

📌 عشان كده أي «لوحة تحكم تعمل أي معادلة» لازم تستنى `firestore.rules` تتصلح الأول.

---

## 30) 📬 نظام الإشعارات (اتنفذ — بعد عناء)

### أ) اللي كان موجود

`index2.html` كان بينده Web3Forms من المتصفح. تلات مشاكل:

1. **المفتاح في كلارتكست على صفحة عامة** — أي حد يستخدم حصة المدرسة
2. **المستقبل ثابت** عند Web3Forms، مش قابل للتغيير
3. **بيانات أطفال لطرف تالت** — اسم الطفل وتليفون ولي الأمر

### ب) 🔴 المحاولة الفاشلة — والدرس الأهم في اليوم

اتكتبت أول مرة كـ`onDocumentCreated` على `registrations`. **النشر فشل:**

```
Location europe-north2 is not found or access is unauthorized
failed to create function .../europe-north2/functions/varsleNySoknad
```

**القاعدة في `europe-north2`.** دالة Firestore **لازم** تتعمل في نفس منطقة القاعدة، والمنطقة دي مش بتدعم Cloud Functions، ومنطقة القاعدة **مش قابلة للتغيير بعد الإنشاء**.

📌 **يعني دوال Firestore مستحيلة في المشروع ده — نهائيًا، مهما اتكتبت إزاي.**

⚠️ الفشل ده كان **غير مرئي في GitHub Actions**: نشر Hosting نجح على نفس الـcommit، فالقايمة بانت خضرا. الخطأ الحقيقي ما بانش غير من `firebase deploy --only functions` على الجهاز.

### ج) الحل المنفّذ — `onSchedule`

`varsleNySoknad` بقت **دالة مجدولة كل دقيقتين**، نفس نمط `ukentligSikkerhetskopi` اللي بتتنشر عادي في `us-central1`.

**المنطق:**
1. تجيب `registrations` وتفلتر اللي `varselSendt !== true`
2. **أول تشغيلة:** تعلّم كل الطلبات القديمة **من غير ما تبعت حاجة** (حارس `varselInitiert`)
3. بعد كده: كل طلب جديد → إشعار للإدارة + إيصال لولي الأمر → تعليم
4. الإرسال فشل؟ الطلب **يفضل غير معلّم** ويتجرّب تاني بعد دقيقتين

📌 **الترتيب مقصود:** التعليم بيحصل **بعد** ما الإيميل يتحط في الطابور. إشعار متكرر أهون من إشعار ضايع.

⚠️ **تأخير لحد دقيقتين.** `every 1 minutes` أصغر وحدة ممكنة في Cloud Scheduler.

### د) 🔧 طريقة نشر الدوال اللي اشتغلت

GitHub Action بتفشل. النشر من الجهاز نجح بالخطوات دي:

```
cd المسار\madina-2026-08-08
cd functions
npm install
cd ..
firebase deploy --only functions
```

⚠️ **تلات فخاخ اتقابلنا معاها:**

1. **`npm install` إجباري** — `node_modules` مش في الأرشيف. من غيره: `Couldn't find firebase-functions package`
2. **`.env.madina-instituttet` مش في الأرشيف** (الملفات المخفية بتتستبعد). من غيره الأمر بيسأل عن القيم تفاعليًا. محتواه:
   ```
   VIPPS_ENV=test
   VIPPS_MSN=465281
   SITE_BASE_URL=https://madinaskole.no
   ```
   🔴 **متستخدمش `.env.example`** — فيه `SITE_BASE_URL` بتاع GitHub Pages الميت، وده بيودّي أولياء الأمور لصفحة 404 بعد الدفع.
3. **Notepad بيزوّد `.txt`** — اختار «Alle filer» في خانة النوع، أو صلّحه بـ`ren`

⚠️ **`--only functions` إجباري.** من غيرها بينشر الموقع كله من ملفات الجهاز.

### هـ) الحالة النهائية

- Web3Forms **اتشال نهائيًا** من `index2.html` — المفتاح مابقاش موجود في الموقع
- كل طلب = إيميلين: واحد للإدارة (اللي مختارين في اللوحة) وواحد لولي الأمر
- **اتجرّب 3 مرات ونجح**

📌 نص الإيصال بيقول صراحة: **«Den er ikke et vedtak om skoleplass»** — عشان ولي الأمر ما يفهمهوش قبول.

---

## 31) 📝 تعديلات على أقسام قديمة

### القسم 5 (App Check) — **اتحل**، شوف القسم 28

### القسم 2 (جدول آخر تعديل)
| الملف | الحالة |
|---|---|
| `foreldreinnsyn.html` · `larerinnsyn.html` | ✅ **اتصلحوا** — مابقاش مكسورين |
| `admin.html` | مجلدات Oversikt · كارت Søskenrabatt · كارت Varslinger |
| `index2.html` | إصلاح Safari · Web3Forms اتشال |
| `functions/index.js` | أسعار من القاعدة · `varsleNySoknad` |
| `publish-tool.html` | حماية النشر · اللوج · تنزيل/نسخ · ZIP |
| `appcheck-test.html` | 🆕 ملف جديد |

### القسم 6 (عطل Safari)
`initializeAppCheck` بقى في `try/catch` في **كل الـ8 ملفات** اللي بتستخدمه — مفيش ملف مكشوف.

🔴 **وعطل رابع مكنش في الملخص:** `foreldreinnsyn` و `larerinnsyn` مكتوب فوقهم «kun for eier» بس الكود كان بيقبل **أي حساب في Firebase Auth**. أي معلم كان يشوف نشاط كل زمايله، وأي ولي أمر يشوف كل الأهالي. اتحل بـ`TILLATTE_EPOSTER`.

⚠️ التحقق ده **في المتصفح** — الحماية الحقيقية لسه محتاجة `firestore.rules`.

### القسم 9 (الباگ المتكرر: onSnapshot قبل الدخول)
**اتكرر للمرة الخامسة:** مستمع `settings/betaling` في `admin.html` كان مربوط على المستوى الأعلى، فخانة رابط فيبس كانت **بتفضل فاضية مهما كان المحفوظ**. اتنقل جوه `attachAuthGatedCashierListeners()`.

### القسم 16 (المتبقي قبل الإطلاق)
**خلص:** ~~App Check~~ · ~~`foreldreinnsyn` و `larerinnsyn`~~ · ~~إشعارات التسجيل~~

**لسه:**
1. 🔴 رقم حساب الفواتير (`INVOICE_BANK_PLACEHOLDER` لسه `1234.56.78900`)
2. 🔴 تناقض نص الخصوصية مع الواقع التقني
3. 🔴 حسم نسب خصم الإخوة (القسم 25)
4. 🟡 `firestore.rules` + `programs` · `schoolCalendar` · `settings`
5. 🟡 باقي قيم القسم 29-ج
6. 🟡 ختم وقت التعديل (القسم 26-ج)
7. 🟡 App Check للـFunctions
8. 🟡 `"node": "22"` في `functions/package.json` — **Node 20 بيتقفل 30 أكتوبر 2026**
9. 🟡 GitHub Action للدوال لسه بتفشل — النشر يدوي حاليًا
10. 🟢 امسح `påmelingsskjema/påmelingsskjema/` (ملف 1 بايت، ومجلد فيه `å`)
11. 🟢 امسح `index-homepage-final.html`
12. 🟢 `pages build and deployment` لسه بتشتغل رغم إن Pages مقفولة

### القسم 18 (ملاحظات دائمة) — أضف
9. **القاعدة في `europe-north2`** — دوال Firestore مستحيلة. `onSchedule` بس.
10. **أي أداة بتعالج ملفات وبتنشر نفسها** لازم تتسأل: هتعمل إيه لو اشتغلت على كودها هي؟
11. **صناديق الحالة تتظهر بالـclass مش بـ`style.display`** — خلط الاتنين بيخلي `display:none` الـinline يغلب `.status.show`، والصندوق يتملّي نص ويفضل مخفي.
12. **الملف الكبير مايتكتبش من الأول** — النسخة في الريبو ممكن تكون أحدث من اللي معاك.
13. **نجاح GitHub Action مش دليل** — Hosting و Functions workflows منفصلين. واحدة تنجح والتانية تفشل على نفس الـcommit.

---

## 32) 🗓️ اللي حصل يوم 8 أغسطس

| # | الحدث | النتيجة |
|---|---|---|
| 1 | `foreldreinnsyn` + `larerinnsyn` مكسورين | ✅ اتصلحوا |
| 2 | الصفحتين بيقبلوا أي حساب | ✅ اتقفلوا على المالك |
| 3 | حماية النشر من النسخ القديمة | ✅ 3 طبقات |
| 4 | لوج + تنزيل/نسخ + ZIP | ✅ اتعملوا |
| 5 | أداة النشر بتخصي نفسها | ✅ اتكشف واتصلح |
| 6 | App Check 0% | ✅ **اتحل** — الدومين |
| 7 | عطل Safari في 8 ملفات | ✅ اتقفل بالكامل |
| 8 | الأسعار متفرقة بين اللوحة وفيبس | ✅ مصدر واحد |
| 9 | إشعارات التسجيل | ✅ بعد اكتشاف `europe-north2` |
| 10 | مفتاح Web3Forms مكشوف | ✅ اتشال |
| 11 | Oversikt رحلة صيد | ✅ 6 مجلدات |

**دروس اليوم:**

- **الدليل من الجهاز حسم كل حاجة.** App Check اتحدد من رسالة حمرا صغيرة في ركن الشاشة. وفشل النشر ما بانش غير من `firebase deploy` على الجهاز — GitHub كان بيقول أخضر.
- **الأداة اللي بتنشر نفسها حالة خاصة** — الباگ عاش 24 ساعة وهو غير مرئي.
- **فحص الـsyntax مسك ملفين مكسورين في يوم واحد.** الإضافات ابتلعت سطر تعريف دالة مرتين.
- **الفشل الصامت هو النمط المتكرر في المشروع ده.** App Check · الأختام · onSnapshot · صناديق الحالة · نشر الدوال — كلهم كانوا بيفشلوا من غير رسالة. أي ميزة جديدة لازم يتسأل عنها: **لو دي فشلت، هأعرف إزاي؟**
- **متكتبش ملف كامل وإنت مش شايف آخر نسخة منه.**
