<!-- SIST-ENDRET: 2026-08-16 03:20:47 -->
# إرشادات الوكلاء (AGENTS.md)

## ملف السياق الرئيسي — اقرأه الأول
قبل أي شغل في المستودع ده، ارجع لملف **`PROJECT-SUMMARY-AR.md`** — ده «ذاكرة المشروع» الكاملة (حقائق دائمة، تحذيرات حية، قيود، وحالة الملفات). وملف **`SUMMARY-ADDITIONS-2026-08-08.md`** إضافات مؤقتة المفروض تتدمج فيه.

## 🔒 قاعدة إلزامية: ختم «آخِر تعديل» في كل ملف يُنشر

### العلامة
اسم العلامة الحرفي = الكلمة `SIST-` ملتصقة فورًا بـ `ENDRET` (مكتوبة هنا مقسّمة **عن قصد** — شوف تحذير الاستبدال العام في الآخر). في الأمثلة تحت بنرمز للكلمة دي بـ `‹العلامة›`.

### النمط اللي بتقراه الأداة بالظبط
```
‹العلامة›: YYYY-MM-DD HH:MM:SS
```
- التاريخ `YYYY-MM-DD` **إجباري**، والثواني `:SS` **إجبارية**. ساعة 24.
- امسح الختم القديم بنفس نوعه قبل ما تكتب الجديد؛ متكوّمش أختام فوق بعض.

### 🕒 التوقيت: Europe/Oslo — مش UTC
الأداة (`noTid()` في `publish-tool.html`) بتستعمل `getHours()`، يعني **التوقيت المحلي للجهاز = أوسلو**. لكن وكيل Cursor شغّال على جهاز سحابي توقيته **UTC** في الغالب، والفرق صيفًا **ساعتين** (CEST = UTC+2).

لو ختمت بـUTC، ملف رفعته الساعة 14:00 بتوقيت أوسلو هياخد ختم `12:00`. وفحص `eldre` بيقارن **ختم بختم**، فالملف الأحدث فعليًا هيبان **أقدم** — والأداة تمنعك أو تسيبك تدوس فوق شغل أحدث، **بصمت**. (الأداة نفسها محذّرة في تعليقاتها إنك ماتقارنش ختم بتاريخ commit لأن ده محلي وده UTC.)

**فولّد الوقت بتوقيت أوسلو دايمًا:**
```
TZ='Europe/Oslo' date '+%Y-%m-%d %H:%M:%S'
```

### الغلاف والمكان حسب النوع
`‹العلامة›` = الكلمة الحرفية من فوق. `<الوقت>` = التاريخ والوقت بتوقيت أوسلو.

| النوع | الشكل | المكان |
| --- | --- | --- |
| `.html` | `<!-- ‹العلامة›: <الوقت> -->` | **بعد `<!DOCTYPE ...>` مباشرة** (أو أول الملف لو مفيش doctype) |
| `.md` | `<!-- ‹العلامة›: <الوقت> -->` | أول الملف (تعليق HTML عشان مايظهرش كعنوان) |
| `.js` و `.css` | `/* ‹العلامة›: <الوقت> */` | أول الملف |
| `.rules` | `/* ‹العلامة›: <الوقت> */` | **السطر ٢، تحت `rules_version` مباشرة** — فوقها بتبوّظ القواعد كلها |
| `.yml` و `.yaml` | `# ‹العلامة›: <الوقت>` | أول الملف |
| `.json` | **بدون ختم** | التعليقات بتبطّل صلاحية JSON |
| غير كده | بدون ختم | سيب المحتوى زي ما هو |

## ⚠️ تحذير: الاستبدال العام بياكل العلامة من أي مكان
قبل ما تكتب ختم جديد، الأداة بتمسح **أي** ظهور للعلامة بـ`replace(/…/g, '')` — عام، مش أول الملف بس. يعني لو كتبت نص العلامة الحرفي جوه **تعليق شرح** أو **توثيق** داخل أي ملف، هيتاكل في أول نشرة.

ده حصل فعلًا: يوم **٧ أغسطس** الأداة مسحت قالب العلامة من **كودها هي**، وكل ملف اتنشر بعدها أخد ختم **فاضي** — والأداة بتقول «✓ Publisert» كل مرة. عشان كده القالب في الكود مكتوب مقسّم: `MERKE_START = '<' + '!--'`.

**القاعدة:** أي مكان تذكر فيه العلامة (توثيق، تعليق، أمثلة) **قسّمها لحتت** أو استخدم رمز نائب زي `‹العلامة›` — زي ما الملف ده عامل. العلامة الحرفية الكاملة تتكتب في **مكان واحد بس**: سطر الختم الحقيقي أول الملف.

## بيئة التطوير (Cloud Agent)
معرّفة في `.cursor/environment.json`: بتثبّت `firebase-tools` وتعمل `npm ci` في `functions/`، وبتشغّل الموقع الثابت على محاكي Firebase Hosting (المنفذ 5000). فحص الصفحات قبل النشر: `node sjekk-sider.mjs`.

## أسماء الملفات
أسماء ملفات المستودع لازم تكون إنجليزي؛ الاسم العربي بيكسر خطوة checkout في GitHub Actions ويوقف كل النشر. المحتوى العربي جوّه الملف عادي.
