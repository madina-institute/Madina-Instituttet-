<!-- SIST-ENDRET: 2026-08-16 01:14:05 -->
# إرشادات الوكلاء (AGENTS.md)

## ملف السياق الرئيسي — اقرأه الأول
قبل أي شغل في المستودع ده، ارجع لملف **`PROJECT-SUMMARY-AR.md`** — ده «ذاكرة المشروع» الكاملة (حقائق دائمة، تحذيرات حية، قيود، وحالة الملفات). وملف **`SUMMARY-ADDITIONS-2026-08-08.md`** إضافات مؤقتة المفروض تتدمج فيه.

## 🔒 قاعدة إلزامية: ختم «آخِر تعديل» في كل ملف يُنشر
أي ملف تعدّله وتنشره/ترفعه، لازم تحط فيه ختم `SIST-ENDRET` بالتاريخ والوقت بصيغة `YYYY-MM-DD HH:MM:SS`، **وتستبدل** أي ختم قديم بدل ما يتراكم (متزوّدش سطر جديد فوق القديم). ده نفس اللي بتعمله أداة `publish-tool.html` بالظبط.

شكل الختم ومكانه حسب نوع الملف:

| النوع | شكل الختم | المكان |
| --- | --- | --- |
| `.html` | `<!-- SIST-ENDRET: <الوقت> -->` | بعد `<!DOCTYPE ...>` مباشرة (أو أول الملف لو مفيش doctype) |
| `.md` | `<!-- SIST-ENDRET: <الوقت> -->` | أول الملف (تعليق HTML عشان مايظهرش كعنوان) |
| `.js` و `.css` | `/* SIST-ENDRET: <الوقت> */` | أول الملف |
| `.rules` | `/* SIST-ENDRET: <الوقت> */` | **بعد** سطر `rules_version` (مش فوقه — وإلا القواعد تبقى غير صالحة) |
| `.yml` و `.yaml` | `# SIST-ENDRET: <الوقت>` | أول الملف |
| `.json` | **بدون ختم** | التعليقات بتبطّل صلاحية JSON |
| غير كده | بدون ختم | سيب المحتوى زي ما هو |

ملاحظات:
- الوقت بصيغة 24 ساعة، بدون منطقة زمنية داخل النص (زي باقي أختام المشروع).
- امسح الختم القديم بنفس نوعه قبل ما تكتب الجديد؛ متسبش أسطر فاضية متراكمة.
- الأختام دي هي المرجع الحقيقي لـ«أحدث نسخة»، ونظام النشر بيمنع رفع نسخة أقدم فوق أحدث بناءً عليها.

## بيئة التطوير (Cloud Agent)
معرّفة في `.cursor/environment.json`: بتثبّت `firebase-tools` وتعمل `npm ci` في `functions/`، وبتشغّل الموقع الثابت على محاكي Firebase Hosting (المنفذ 5000). فحص الصفحات قبل النشر: `node sjekk-sider.mjs`.

## أسماء الملفات
أسماء ملفات المستودع لازم تكون إنجليزي؛ الاسم العربي بيكسر خطوة checkout في GitHub Actions ويوقف كل النشر. المحتوى العربي جوّه الملف عادي.
