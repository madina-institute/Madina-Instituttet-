<!-- SIST-ENDRET: 2026-08-10 11:55:04 -->
# ملخص مشروع مدرسة مدينة — نقطة الاستكمال

**آخر تحديث: 10 أغسطس 2026 — بعد منتصف الليل**

الصق الملف ده في أول رسالة في محادثة جديدة مع Claude عشان يفهم السياق كامل.

---

## 0) 🔴 اقرأ ده الأول

### أ) أربع حقائق دائمة — تخص الكود نفسه

| # | الحقيقة | النتيجة |
|---|---|---|
| 1 | **القاعدة في `europe-north2`** | دوال Firestore (`onDocumentCreated`) **مستحيلة في المشروع ده**. المنطقة مش بتدعم Cloud Functions، ومنطقة القاعدة مش قابلة للتغيير بعد الإنشاء. البديل الوحيد: `onSchedule`. كلّفت ساعتين يوم 8 أغسطس. |
| 2 | **قالب الختم متجمّع من قطع** | `MERKE_START = '<' + '!--'` في `publish-tool.html`. لو اتكتب كسلسلة واحدة، الأداة بتمسحه من كودها هي لما تنشر نفسها. |
| 3 | **Firebase Extensions بتتقفل 31 مارس 2027** | كل الإيميلات ماشية على `firestore-send-email`. دليل الترحيل من Google في سبتمبر 2026. |
| 4 | **`--only functions` إجباري** | `firebase deploy` من غيرها بينشر الموقع كله من ملفات الجهاز، اللي غالبًا قديمة. |
| 5 | **مفتاح الطوارئ في `firestore.rules`** | إيميل المالك مكتوب في `erEier()`. ده الطريق الوحيد للرجوع لو جدول الصلاحيات اتكسر. **متشيلوش أبدًا.** |
| 6 | **`varselSendt: false` لازم تتكتب في `index2.html`** | Firestore مايقدرش يسأل عن حقل ناقص. من غيرها الطلبات الجديدة مش هيتبلّغ عنها. |

### ب) تحذيرات حية — تحتاج قرار أو تنفيذ

| # | التحذير | ليه |
|---|---|---|
| 1 | 🟡 **رقم حساب الفواتير مش مسجّل** | اتشال من الكود (9 أغسطس). بقى في `settings/faktura` ويتدخل من Betalingsinnstillinger. **لسه فاضي** — والفاتورة بترفض تتبعت لحد ما يتحط، بفحص MOD-11 |
| 2 | ⚠️ **نص الخصوصية** | الفجوة ضاقت: الحقول الصحية بقت **مموّهة بنجوم** ومقفولة في التصدير (القسم 43). **بس ده في الشاشة مش في القاعدة** — اللي يفتح الكونسول لسه بيقراها |
| 3 | ✅ ~~نسب خصم الإخوة متضاربة~~ | اتحسم: **10% و15%** في `settings/priser`، ومفيش رقم في الكود |
| 4 | ⚠️ **متحوّلش App Check لـ`Enforced` لسه** | اتصلح 8 أغسطس وبقى بيصدر توكنات، **بس** فيه ~7% من الطلبات مش هتعدي أبدًا (متصفحات بتحجب reCAPTCHA). الشرط: فوق 90% ثابت كام يوم، بنطاق 24 ساعة |
| 5 | ⚠️ **متعملش QR من Canva** | كود مريم كان `canvaqr.com` وسيط بيوجّه لعنوان GitHub Pages الميت |

### ج) 🧠 النمط الحاكم للمشروع: الفشل الصامت

**كل عطل اتصلح يوم 8 أغسطس كان بيفشل من غير ما يقول:**

| العطل | الشكل الظاهر | مدة الصمت |
|---|---|---|
| App Check 0% | كل حاجة شغالة | **4 أيام** |
| الأداة بتخصي نفسها | «✓ Publisert» | **24 ساعة** |
| نشر الدوال فاشل | GitHub كله أخضر | يومين |
| مستمعو اللوحة | «مش متصل» + refresh بيصلحها | من البداية |
| فيبس: بانر «منتظر الدفع» | الدفع نجح فعلًا | من البداية |
| زرار النشر بلا رد | الرفع نجح | ساعة |

📌 **القاعدة:** أي ميزة جديدة، اسأل — **لو دي فشلت، هأعرف إزاي؟**

📌 **وسبب متكرر تاني:** الكود بيفحص شرط **قريب** من الصح بدل الصح نفسه. حصل 3 مرات في يوم واحد في أماكن مختلفة تمامًا. أمثلة في القسم 35.

---

## 1) نظرة عامة

- **المدرسة**: Madina Skole — مدرسة إسلامية في النرويج (قرآن/إسلام/عربي/أردو)
- **الدومين**: `madinaskole.no` — والاحتياطي `madinaskolen.no` (بيحوّل)
- **مشروع Firebase**: `madina-instituttet`
- **الريبو**: `madina-institute/Madina-Instituttet-` (فرع `main`، **عام**)
- **مزوّد الدومين**: Webhuset (`webhuset.no`) — الدومينين في نفس الحساب
- **البنية**: ملفات HTML مستقلة (بدون framework) + Firestore + Cloud Functions + Firebase Hosting + Storage
- **المجلد المحلي**: `C:\Users\yasse\iCloudDrive\Disktop\madina-vipps`

⚠️ **أسماء الملفات في الريبو لازم تكون إنجليزي.** اسم عربي بيكسر خطوة checkout في GitHub Actions وبيوقف **كل** النشر (حصل فعلًا مع `ملخص-المشروع-للاستكمال.md`). المحتوى بالعربي جوه الملف عادي.

---

## 2) 🕒 حالة الملفات — 9 أغسطس 2026

⚠️ **الختم جوه كل ملف هو المرجع**، مش الجدول ده. ومن 8 أغسطس 15:22 بقى شغال فعلًا لأول مرة (شوف 27).

| الملف | الحالة |
|---|---|
| `admin.html` | 🔄 اتغيّر جوهريًا — مجموعات جديدة · صلاحيات · كروت جدولية · تصدير · كتالوج · تسجيل جلسات · إصلاح 32 مستمع |
| `publish-tool.html` | 🔄 حماية النشر · لوج · تنزيل/نسخ · ZIP · بانر النسخة القديمة |
| `functions/index.js` | 🔄 أسعار من القاعدة · `varsleNySoknad` · فحص صحة · إصلاح باگ فيبس |
| `index1.html` | 🔄 إكمال بيانات المعلم · Safari · `feillogg` |
| `index2.html` | 🔄 Safari · Web3Forms اتشال |
| `index4.html` | 🔄 Safari · `feillogg` |
| `foreldreinnsyn.html` · `larerinnsyn.html` | ✅ **اتصلحوا** — مابقاش مكسورين |
| `slettelogg.html` · `statistikk.html` | 🔄 Safari · الشارة |
| `appcheck-test.html` | 🆕 تشخيص App Check |
| `helsesjekk.html` | 🆕 فحص الصحة العام |
| `ledelseinnsyn.html` | 🆕 إحصائيات استخدام اللوحة |
| `firestore.rules` | ⚠️ ناقصها `programs` · `schoolCalendar` · `settings` · `feillogg` |

---

## 3) الملفات

| الملف | الوظيفة |
|---|---|
| `admin.html` | لوحة التحكم الكاملة (~8300 سطر) |
| `functions/index.js` | Cloud Functions: فيبس + إيميلات + النسخ الاحتياطي |
| `index.html` | الصفحة الرئيسية (3 لغات) — **مفيهاش Firebase خالص** |
| `index1.html` | بوابة المعلمين |
| `index2.html` | استمارة التسجيل العامة — **الوحيدة اللي بتكتب بدون تسجيل دخول** |
| `index3.html` | البوابة الرئيسية — لا تلمس Firestore |
| `index4.html` | بوابة أولياء الأمور |
| `foreldreinnsyn.html` / `larerinnsyn.html` | بوابات استعلام (للمالك) |
| `timeplan.html` / `kunngjoring.html` | الجدول / الإعلانات — لا تلمس Firestore |
| `madina-brosjyre.html` | بروشور تفاعلي بـ3 لغات |
| `publish-tool.html` | أداة النشر (GitHub API) |
| `slettelogg.html` / `statistikk.html` / `personvern.html` | سجل حذف / إحصائيات / خصوصية |
| `firestore.rules` | **توثيق فقط** — القواعد الفعلية في Firebase Console |
| `sw.js` | Service Worker — network-first لـHTML |
| `.github/workflows/firebase-hosting.yml` | نشر تلقائي للموقع (~40 ثانية) |
| `.github/workflows/firebase-functions.yml` | نشر تلقائي للـFunctions (~2-5 دقائق) |

⚠️ **ملف يتيم:** `index-homepage-final.html` موجود في الريبو ومنشور على الإنترنت، مش مربوط من أي مكان. غالبًا مسودة قديمة — **افحصه وامسحه.**

---

## 4) 🌐 الدومينات والـDNS (اتصلح 7 أغسطس)

### الوضع الحالي
```
A      @      → 199.36.158.100        (Firebase Hosting)
A      www    → [يحتاج تأكيد]          Firebase: "Minting certificate"
A      ftp    → 46.226.10.76          (Webhuset — قديم)
MX     @      → Zoho (3 سجلات)
CNAME  brevo1/brevo2/mail/img/r       (Brevo — إيميلات)
CNAME  firebase1/firebase2            (Firebase — إيميلات)
TXT    firebase= · hosting-site= · SPF · DMARC · Zoho · Brevo
```

### 🔴 المشكلة اللي اتكشفت 7 أغسطس
كان فيه `CNAME www → madina-institute.github.io` **فاضل من أيام GitHub Pages**. الملخص القديم كان بيقول إن سجلات Pages اتمسحت — اتمسحت من الجذر بس، و`www` اتنسي.

النتيجة: **أي حد يكتب `www.madinaskole.no` كان بيشوف صفحة 404 بتاعة GitHub.**

**الحل المطبّق:** السجل اتشال، و`www.madinaskole.no` اتضاف كدومين مخصص في Firebase Hosting.

### `madinaskolen.no` (بالـn — دومين احتياطي)
- مملوك لنفس الحساب في Webhuset
- **Web → Peker → URL-viderekobling → `https://madinaskole.no`**
- ⚠️ التحويل ده **مش بيحافظ على المسار** — بيوديّ للصفحة الرئيسية بس. مقبول لأنه احتياطي
- ⚠️ **`www.madinaskolen.no` لسه في قايمة Firebase بحالة "Needs setup"** — لازم يتمسح، هيفضل يطلع تحذير كاذب

---

## 5) ✅ App Check — اتحل 8 أغسطس

**السبب كان سطر واحد:** مفتاح reCAPTCHA كان مسجّل عليه `madina.no` بس — والموقع على `madinaskole.no`. دومين مختلف تمامًا، مش حتى GitHub Pages القديم زي ما كان متوقع.

**التفاصيل الكاملة والأرقام قبل الإصلاح: القسم 28.**

🔴 **لسه ممنوع `Enforced`** — شوف القسم 0-ب.

---

## 6) 🍎 عطل Safari — بوابة أولياء الأمور (اتصلح 7 أغسطس)

### الأعراض
- ولي أمر بيسجّل دخول من **Firefox** → يشتغل
- نفس الحساب من **Safari** → «Fant ingen foresatt-konto» (الحساب موجود!)
- Safari بيطلع بانر: «Hvis siden ikke vises som forventet, kan redusering av avanserte personvernbeskyttelser...»
- ضغط «Reduser beskyttelse» → يشتغل

### السبب الجذري
Safari بـ«الحماية المتقدمة» بيحجب `google.com/recaptcha`. الحجب بيخلي `initializeAppCheck` **يرمي خطأ**، والخطأ بيوقف تنفيذ **الموديول كله** — فالصفحة تظهر كاملة وتفضل ميتة من جوه.

### الحل المطبّق في `index4.html`
1. **`initializeAppCheck` جوه `try/catch`** — الفشل مابيوقفش الموديول
2. **عكس ترتيب الدخول** — دخول أولًا، بعدين `getDocs` للبحث، ولو مش موجود `signOut`
3. **20 مستمع اتحوّلوا لـ`watch()`** — مؤجلين لبعد تأكيد الدخول
4. **`location.reload()` عند الخروج**

⚠️ **الملفات دي لسه فيها نفس العطل:** `foreldreinnsyn.html` · `larerinnsyn.html`
- `foreldreinnsyn` مكسور **دايمًا** (فيه `inMemoryPersistence` فالجلسة بتتمسح كل مرة)
- `larerinnsyn` مكسور **بشكل متقطع** (بيشتغل لو الجلسة اترجّعت قبل المستمعين) — وده أصعب في التشخيص

---

## 7) 📱 كود QR — درس مهم

**كود Canva بتاع الإعلان كان بيوديّ:**
```
https://www.canvaqr.com/RGPbCWkOnN
```

الكود **مافيهوش عنوان الموقع خالص** — فيه وسيط بيوجّه لعنوان GitHub Pages القديم اللي اتقفل يوم 4 أغسطس. فأي حد يمسحه بيشوف صفحة 404.

### قاعدة دائمة
**متستخدمش QR ديناميكي من أي خدمة.** استخدم مولّد **Static** (زي `qrcode-monkey.com`) بحيث العنوان نفسه يبقى مخزّن جوه الكود.

**طريقة الفحص:** افتح صورة الكود على الموبايل → اضغط مطوّل → شوف الرابط. لازم يبان `madinaskole.no` مباشرة.

**الأكواد المؤكدة سليمة:**
- `https://madinaskole.no/index2.html` ✅ — ده اللي يتحط في الإعلانات (بيوديّ للاستمارة مباشرة)
- `http://madinaskole.no` ⚠️ — سليم بس بـ`http` مش `https`

**ليه ده خطير:** الوسيط ممكن يغيّر الوجهة في أي وقت، وبيسجّل كل مسحة عند طرف تالت (مشكلة GDPR في مدرسة نرويجية).

---

## 8) البنية التحتية والنشر ✅

الموقع على **Firebase Hosting**. **GitHub Pages اتقفل نهائيًا** (Unpublish) في 4 أغسطس.

- `firebase.json` فيه `no-cache` لملفات HTML
- `public: "."` — **كل ملف في جذر الريبو بيتنشر على الإنترنت** (بما فيهم `firestore.rules`)
- المستثنى: `firebase.json` · المخفية · `node_modules` · `functions/**` · `*.md`
- Secret اسمه `FIREBASE_TOKEN` في GitHub → Settings → Secrets → Actions

### `sw.js` — Service Worker
**network-first لملفات HTML** مع `cache: 'no-store'` — يعني أي نشر بيوصل للأجهزة **فورًا**، ومش محتاج تغيّر `?v=4`. الكاش بيتستخدم أوفلاين بس.

### أداة النشر — محدّثة 7 أغسطس
**القايمة بقت بتتبني من الريبو** عن طريق `GET /repos/{owner}/{repo}/git/trees/main?recursive=1`.

- أي ملف جديد ترفعه بـ«Annen filsti» بيظهر في القايمة تلقائيًا بعدها
- لو GitHub ما ردّش → بترجع للقايمة القديمة تلقائيًا (مستحيل تقع)
- **الرفع المتعدد بقى بيصحّح المسارات** — كان `index.js` بيروح للجذر وبيقول «✓ Publisert» كذبًا
- **خانة وصف التعديل** بتظهر في تاريخ GitHub

⚠️ **الاختيار الافتراضي بيقع على «Annen filsti»** بدل آخر ملف مرفوع — باگ صغير مش متصلح.

⚠️ **التوكن في `localStorage`** بصلاحية كتابة على الريبو. أي حد يوصل للمتصفح يقدر يعدّل الريبو.

---

## 9) 🔒 الأمان

### الحالة الحالية
كل `read: if true` بقت `read: if request.auth != null`، ماعدا:
- `registrations` → `create: if true` (الاستمارة العامة)
- `formAnalytics` → `create: if true` (عداد الزيارات)

و `match /{document=**} { allow read, write: if false; }` في الآخر.

### 🔴 `firestore.rules` ناقصه مجموعتين
الملف في الريبو **مفيهوش `programs` ولا `schoolCalendar`**. لو القواعد المنشورة في الكونسول مطابقة له، التبويبين دول هيطلعوا فاضيين.

**الاختبار:** افتح اللوحة وبص على الشارة فوق. «✓ Synkronisert» = الكونسول محدّث. «Feil: programs» = القواعد ناقصة.

### 🟡 ثغرات معروفة في القواعد الحالية
- **`mail`** مفتوحة للقراءة لأي حساب مسجّل — فيها نص كل الإيميلات (تقارير دفع، فواتير، أرقام حساب)
- **`deletionLog`** مفتوحة للقراءة — فيها **نسخة كاملة** من كل سجل اتمسح

### 🐛 الباگ المتكرر: onSnapshot قبل تأكيد الدخول

**أكتر غلطة اتكررت في المشروع — 4 مرات.**

الـ Firestore SDK **لا يعيد المحاولة** لما القراءة تترفض. فأي `onSnapshot` بيتربط قبل تأكيد الدخول بيموت **بشكل دائم**.

**الملفات اللي اتصلحت:** `index1` · `slettelogg` · `statistikk` (6 أغسطس) · `index4` (7 أغسطس)
**لسه مكسور:** `foreldreinnsyn` · `larerinnsyn`

📌 **قاعدة دائمة: أي `onSnapshot` جديد لازم يمر على `watch()`.**

---

## 10) 🎯 نمط التأمين الصحيح (مرجع)

`functions/index.js` فيه أقوى تأمين في المشروع — **يستخدم كمرجع لأي حاجة جاية**:

```js
const OPPTAK_ROLLER = ["Eier", "Opptaksansvarlig"];
async function krevOpptaksrolle(bruker, res) {
  const snap = await db.collection("adminUsers")
    .where("epost", "==", bruker.epost).get();
  // ... يتحقق من الدور على السيرفر
}
```

**الفرق المهم:** إخفاء الأزرار في `admin.html` بيحصل في المتصفح ويتعمله التفاف. التحقق ده بيحصل **على السيرفر** — طلب فيبس مايتعملش إلا لو السيرفر قرا `adminUsers` ولقى الدور.

`krevInnlogget()` بيتحقق من Firebase ID token في `Authorization: Bearer`.

---

## 11) 📅 البرامج والتقويم المدرسي

### `programs` — تبويب «📅 Programmer»
كل برنامج: الاسم · أيام التدريس · السعر · مفعّل

- `Madina Islamske Skole — Integrert program (Lørdag)` → 2750
- `Madinabarn (Søndag)` → 1500

⚠️ **الربط بالاسم** — تغيير الاسم بيكسر ربط الصفوف والطلاب.

### `schoolCalendar` — تبويب «🗓️ Skolekalender»
- **`Fri`** يقفل يوم مفتوح
- **`Ekstra undervisningsdag`** يفتح يوم مقفول
- **اليوم الإضافي يغلب الإجازة** لو اتعارضوا

📌 **الاحتياطي دايمًا بيضيّق مش بيوسّع** — لو البرنامج مفقود، بيرجع للسبت/الأحد.

---

## 12) 💰 الأسعار والخصومات

### ⚠️ تضارب لازم يتحسم
| المكان | الطفل 2 | الطفل 3+ |
|---|---|---|
| `admin.html` | 20% | 30% |
| `functions/index.js` | 20% | 30% |
| البروشور المنشور | 20% | 30% |
| **`rabattforslag.html`** | **10%** | **20%** |

الكود متسق. **مستند المقترح اللي رايح للإدارة فيه أرقام تانية.**

### 🟡 حساب الإخوة ما بيعدّش `foresatt2`
الاتنين (`admin.html` و `functions/index.js`) بيعدّوا من `foresatt` بس. لو الطفل مسجّل تحت ولي الأمر التاني، **مش هيتحسب كأخ والعيلة تدفع أكتر**.

### الأسعار الخاصة
- **كارت المعلم:** `Egen timesats` + `Begrunnelse`. **«متطوع» يغلب على كل حاجة** — حتى لو فيه رقم، الراتب صفر
- **كارت الطالب:** `Spesialpris` + `Begrunnelse`. محمي من إعادة الحساب التلقائي

---

## 13) 🗂️ لوحة التحكم — المجلدات (7 أغسطس)

أربع أقسام بقى فيها مجلدات بنفس النمط: **الطلبات · الطلاب · المعلمين · أولياء الأمور**

| الحالة | اللي يظهر |
|---|---|
| فتحت التبويب | المجلدات وأعدادها بس |
| دوست على مجلد | البحث + الأدوات + القايمة مفلترة |
| **📋 Alle** | كل حاجة |
| نفس المجلد تاني | يتقفل |

**التقسيمات:**
- **الطلبات:** Alle · Nye · Venteliste · Godkjent · Avslått
- **الطلاب:** Alle · مجلد لكل صف · Uten klasse · Sluttet/trukket
- **المعلمين:** Alle · مجلد لكل مادة (من الداتا) · Uten fag
- **أولياء الأمور:** Alle · مجلد لكل صف (حسب صف الابن) · Uten barn i klasse

📌 ولي الأمر اللي عنده ولدين في صفين بيظهر في المجلدين — مقصود. بيحسب من `foresatt` و`foresatt2` سوا.

⚠️ **درس:** اتلقى في `admin.html` نسختين من نفس الفكرة (دالة `visMappeInnhold` معرّفة مرتين) — ده كان بيمنع الجافاسكريبت من الاشتغال أصلًا. **دايمًا افحص لو الفكرة اتعملت جزئيًا قبل كده.**

---

## 14) الميزات المبنية في admin.html

1. **الكاشير**: المستحق/المدفوع/المتبقي، سجل دفعات، دفعة يدوية، تصدير CSV
2. **Kasserer (Ledelse)** — لدور «Eier» فقط: استرجاع فيبس، إلغاء طلب، خصم، فاتورة
3. **دور `Opptaksansvarlig`** — قرارات القبول محصورة فيه وفي «Eier»
4. **حماية الحذف** — `slettMedBekreftelse()` + نافذة باسورد مقنّعة + `reauthenticateWithCredential`
5. **اقتراحات الربط** — `finnPersonTreff()` بيقترح «ممكن إخوة» بمطابقة تليفون/إيميل، **بس مش بيربط تلقائي**
6. **منع تكرار طلبات فيبس** — `apentVippsKrav()`
7. **تحضير الدروس**: نموذج 60 دقيقة (شوطين) + حقول لكل مادة
8. **نظام الرواتب** الكامل
9. **جلسة دائمة للمالك**: `PERSISTENT_SESSION_EMAILS`
10. **حفظ التبويب في الرابط** (`#fane=`)

⚠️ **المستمعون مكتوبين يدويًا** في قايمة `attachListener(...)` — **مش بيتولّدوا من `SCHEMAS`**.

---

## 15) 💾 النسخ الاحتياطي

دالة `ukentligSikkerhetskopi` بتشتغل **كل أحد 04:00 أوسلو**:
1. تقرا الـ43 مجموعة
2. تكتبهم في JSON في Storage تحت `backups/`
3. تبعت إيميل فيه **اسم الملف** (مش رابط — عن قصد)
4. تمسح الأقدم من 60 يوم

يدوي: `https://us-central1-madina-instituttet.cloudfunctions.net/taSikkerhetskopiNa`

📌 **مفيش رابط تحميل عمدًا** — الرابط الموقّع بيشتغل لأي حد بدون تسجيل دخول، والملف فيه بيانات أطفال.

**آخر نسخة مؤكدة:** 6 أغسطس — 784 مستند · 0.30 MB

---

## 16) ⏳ المتبقي قبل الإطلاق

**خلص 8 أغسطس:** ~~App Check~~ · ~~`foreldreinnsyn` و `larerinnsyn`~~ · ~~إشعارات التسجيل~~ · ~~GitHub Action للدوال~~ · ~~توحيد الأسعار~~

### 🔴 قرارات مطلوبة منك (مش كود)
1. **رقم حساب الفواتير** الحقيقي — الخانة موجودة في Betalingsinnstillinger ومستنياه. الفاتورة بترفض تتبعت من غيره
2. **تناقض نص الخصوصية** مع الواقع التقني
3. ~~حسم نسب خصم الإخوة~~ — **اتحسم: 10% و15%** (القسم 37-ج)

### 🟡 شغل تقني
4. ~~`firestore.rules`~~ — **اتعملت**: قفل 13 مجموعة · تسلسل الأدوار · مفتاح الطوارئ
4b. ~~**أجر الساعة** (200/150) ورقم الحساب في الكود~~ — **اتنقلوا** لـ`settings/lonn` و`settings/faktura` (9 أغسطس)
5. **`feillogg`** تتضاف لـ`BACKUP_COLLECTIONS` و `TYPE_LABELS` (قاعدة الست حاجات — القسم 18)
6. **باقي قيم القسم 24** تخرج لـ`settings` (المدد · الأجور · رقم الحساب)
7. **ختم وقت التعديل** بدل وقت النشر (القسم 26-ج)
8. **App Check للـFunctions** — مش محمية خالص
9. **`"node": "22"`** في `functions/package.json` — **Node 20 بيتقفل 30 أكتوبر 2026**
10. **الترحيل من Firebase Extensions** — **31 مارس 2027**
11. **`VIPPS_ENV=production`** + المفاتيح الحقيقية + رقم التاجر الحقيقي
12. **منطقة الدوال `us-central1`** — الأنضف `europe-west1`

### 🟢 تنضيف
13. امسح `påmelingsskjema/påmelingsskjema/` (ملف 1 بايت، ومجلد فيه `å`)
14. امسح `index-homepage-final.html` · `index3.html`
15. اقفل workflow بتاع GitHub Pages — لسه بيشتغل رغم إن Pages مقفولة
16. امسح `www.madinaskolen.no` من Firebase
17. تنضيف `deletionLog`

### 🔒 المرحلة 2 من الأمان
أي حساب مسجّل يقدر يقرا/يكتب أي حاجة.

⚠️ **ملاحظة معمارية:** التقييد حسب الدور **مش شغل قواعد بس**. البوابات بتحمّل المجموعات كاملة وتفلتر في المتصفح، و Firestore بيرفض الاستعلام **كله** لو فيه مستند واحد ممكن يتعارض. فقاعدة «ولي الأمر يشوف ابنه بس» هتخلي البوابة **فاضية** مش تفلتر. **محتاج إعادة كتابة الاستعلامات.**

### المرحلة 3 (مؤجلة)
ربط المعلم بأكتر من صف. `tildeltKlasse` قيمة واحدة. **بتلمس تسجيل الدخول والصلاحيات.**

---

## 17) ملف الإعدادات

`functions/.env.madina-instituttet`:
```
VIPPS_ENV=test
VIPPS_MSN=465281
SITE_BASE_URL=https://madinaskole.no
```

📌 **قاعدة**: أي قيمة فيها SECRET/KEY/PASSWORD **متتحطش هنا** (الريبو عام) — تروح لـSecret Manager.

⚠️ `.gitignore` بيتجاهل `.env` بالظبط بس — **مش** `.env.madina-instituttet`. الملف ده **منشور على الريبو العام**.

⚠️ **مفتاح Web3Forms مكشوف** في `index2.html`: `93e54814-0408-4748-bf5c-7b00b5778d78`

---

## 18) ملاحظات تقنية دائمة

### أ) قبل ما تسلّم أي كود
1. **افحص syntax** (`node --check`) — مسك ملفات مكسورة 3 مرات في يوم واحد
2. **شيل `SIST-ENDRET`** من أي ملف بيتسلّم — الختم الموروث بيخلي الحارس يمنع شغل صحيح
3. **متكتبش ملف كامل وإنت مش شايف آخر نسخة منه** — النسخة في الريبو ممكن تكون أحدث
4. **قبل تعديل ملف كبير، افحص لو الفكرة اتعملت جزئيًا قبل كده** — تعريف مكرر بيوقف الجافاسكريبت كله

### ب) الفشل الصامت — المصدر الأول للأعطال هنا
5. **اسأل عن كل ميزة: لو دي فشلت، هأعرف إزاي؟**
6. **أي كود على مستوى الموديول بيشتغل قبل الدخول** — مستمعين، استعادة عرض، أي حاجة محتاجة توكن. اتكرر **6 مرات**
7. **أي `catch` بيطبع في الكونسول بس هو عطل صامت مستقبلي** — على الموبايل مفيش كونسول. استخدم `feillogg`
8. **نجاح GitHub Action مش دليل** — Hosting و Functions تشغيلتين منفصلتين على نفس الـcommit
9. **صناديق الحالة تتظهر بالـclass مش بـ`style.display`** — خلط الاتنين بيخلي الصندوق يتملّي نص ويفضل مخفي
10. **أي شاشة انتظار محتاجة مهلة** — من غيرها بتفضل معلقة للأبد لو الرد ما جاش

### ج) فخاخ محددة
11. **القاعدة في `europe-north2`** — دوال Firestore مستحيلة
12. **أي أداة بتنشر نفسها** لازم تتسأل: هتعمل إيه لو اشتغلت على كودها هي؟
13. **`Date.parse(0)` = سنة 2000**، مش صفر
14. **اسأل عن الشرط الصح مش القريب منه** — «فيه حقول مخفية؟» مش «فيه حقول أساسية؟»
15. **أسماء الملفات في الريبو إنجليزي دايمًا**
16. **مسار ملفات الـFunctions لازم يبدأ بـ`functions/`**

### د) قواعد المشروع
17. **مصدر الحقيقة الوحيد هو GitHub**
18. **أي مجموعة جديدة تحتاج 6 حاجات:** قاعدة في `firestore.rules` · `attachListener()` · `cache` · `BACKUP_COLLECTIONS` · `TYPE_LABELS` في `slettelogg` · استخدامها في البوابات
19. **الأدوار والصلاحيات تفضل في الكود** — لو اتنقلت للقاعدة، أي حد يكتب فيها يدّي نفسه صلاحية
20. **أي تغيير في اللوحة، الكتالوج (تبويب Veiledning) يتحدّث في نفس الشغلة**

### باگات صغيرة معروفة
- `index1.html`: نافذة الطباعة بتنادي `notify()` وهي مش معرّفة → `ReferenceError`
- `kunngjoring.html`: اللوجو من `https://madina.no/images/logo.png` (دومين خارجي)
- `publish-tool.html`: الاختيار الافتراضي بيقع على «Annen filsti»

---

## 19) 🗓️ اللي حصل يوم 7 أغسطس

| # | الحدث | النتيجة |
|---|---|---|
| 1 | كود QR بتاع مريم بيوديّ 404 | ✅ اتغيّر لكود مباشر |
| 2 | `CNAME www` فاضل من GitHub Pages | ✅ اتشال + `www` اتضاف لـFirebase |
| 3 | `madinaskolen.no` مالوش وجهة | ✅ Peker → `madinaskole.no` |
| 4 | بوابة الأولياء مكسورة على Safari | ✅ اتصلحت ومجرّبة على iPhone |
| 5 | App Check 0% توكنات صالحة | ⚠️ اتكشف — لسه محتاج إصلاح |
| 6 | مجلدات المعلمين والأولياء | ✅ اتعملت |
| 7 | المجلدات تبدأ مقفولة | ✅ اتعملت |
| 8 | أداة النشر تقرا الريبو | ✅ اتعملت ومجرّبة (31 ملف) |

**دروس من اليوم ده:**
- التشخيص اتغيّر 3 مرات قبل ما يستقر. كل مرة اللي حسم كان **دليل من الجهاز** (سكرين شوت، اختبار حقيقي) مش تحليل الكود
- الملخص القديم كان فيه معلومات **غلط** (قال إن `publish-tool` المحسّنة مش مرفوعة وهي مرفوعة)
- **الملخص يتكتب أول المحادثة مش آخرها**

---

## 20) 🏫 نسخ المشروع لمدرسة تانية

⚠️ **المشروع ده مش متصمّم للنسخ.** القيم متناثرة في الكود بدل ما تكون في ملف إعدادات واحد. تقدير الوقت: **يومين تقريبًا**.

### أ) حسابات جديدة
- مشروع Firebase جديد (Firestore + Auth + Storage + Hosting + **خطة Blaze**)
- دومين جديد + سجل `A` على `199.36.158.100`
- حساب Brevo + توثيق الدومين (DKIM/DMARC)
- ريبو GitHub جديد + `FIREBASE_TOKEN` في Secrets
- حساب Vipps ورقم تاجر (لو هيستخدموا الدفع)

### ب) القيم اللي لازم تتغير في الكود

| القيمة | فين |
|---|---|
| `firebaseConfig` (6 أسطر) | **7 ملفات** — كل بوابة فيها نسخة |
| مفتاح reCAPTCHA | نفس الـ7 ملفات |
| `madina-instituttet` | `.firebaserc` + `firebase.json` + روابط الدوال |
| روابط Cloud Functions | `admin.html` + `index4.html` |
| `PERSISTENT_SESSION_EMAILS` | `admin.html` |
| `post@madinaskole.no` | `admin.html` + `functions/index.js` |
| إيميل النسخ الاحتياطي (`BACKUP_EMAIL`) | `functions/index.js` |
| `GH_OWNER` / `GH_REPO` | **8 ملفات** (شارة النشر) |
| `PROGRAM_PRICES` | `admin.html` + `functions/index.js` |
| نسب خصم الإخوة | نفس المكانين |
| اسم المدرسة/العنوان/الشعار | كل الصفحات |
| `INVOICE_BANK_PLACEHOLDER` | `admin.html` |
| مفتاح Web3Forms | `index2.html` |

### ج) اللي مش في الكود
- **قواعد Firestore** — نسخ لصق يدوي في الكونسول
- **إضافة Trigger Email** — تركيب جديد
- **App Check + reCAPTCHA** — ⚠️ ده اللي مكسور هنا (0%). **متكرروش بنفس الطريقة**
- بيانات أولية: صفوف · برامج · تقويم · حساب المالك

### د) الأصعب — النصوص
النصوص النرويجية والعربية والأردية مكتوبة **جوه الكود** مش في ملف ترجمة منفصل. مدرسة باسم مختلف هتحتاج تعديل يدوي واسع.

### 📌 التوصية
لو النسخ احتمال جدّي، **اعمل `config.js` واحد يجمع كل القيم دي** وكل الصفحات تقرا منه. شغل يوم واحد دلوقتي بيوفر أسبوع لكل نسخة جاية.

---

## 21) ⏱️ نظام التوقيت (7 أغسطس — الليل)

### الحقن
`publish-tool.html` بقت **بتحقن سطر في أول كل ملف** عند الرفع:
```html
```

الصياغة بتختلف حسب النوع: `<!-- -->` لـHTML وMarkdown · `/* */` لـJS/CSS/rules · `#` لـYAML · **JSON بيتخطى** (الكومنت بيكسره).

الختم القديم بيتشال قبل ما الجديد يتحط — مش بيتراكم.

### الشارة
في **8 ملفات داخلية** (`admin` · `index1` · `index4` · `foreldreinnsyn` · `larerinnsyn` · `slettelogg` · `statistikk` · `publish-tool`) شارتين فوق بعض:
```
✏️ Sist endret:      7.8.2026 kl. 23:14:31   ← الملف ده اتعدل امتى
✓ Siste publisering: 7.8.2026 kl. 23:15      ← الموقع اتنشر امتى
```

### الصفحة العامة
`index.html` الشارة فيها **مخفية** — تظهر بس لما تفتح:
```
madinaskole.no/?dev
```
الزائر ما يشوفش حاجة. ولو ظهرت «Ingen tidsstempel — publisert utenom verktøyet»، يبقى الملف اترفع من غير الأداة.

### 📌 الفايدة الأساسية
لو بعتّ ملف في محادثة جديدة وختمه أقدم من اللي في **جدول القسم 2**، Claude هيقولك إن معاك نسخة قديمة — من غير ما تسأل.

---

## 22) ⚠️ استخدام العنوان الغلط لأداة النشر

اتضح إن الأداة كانت بتتفتح من:
```
https://madina-institute.github.io/Madina-Instituttet-/publish-tool.html
```

**ده بيشتغل** لأن الأداة بتكلم `api.github.com` مباشرة، مش الصفحة اللي فاتحها منها. لكنه عنوان GitHub Pages **المقفول** — والصفحة بتتحمّل من كاش المتصفح.

📌 **العنوان الصح:**
```
https://madinaskole.no/publish-tool.html
```
امسح أي بوكمارك أو أيقونة على الشاشة الرئيسية بتشاور على العنوان القديم.

---

## 23) 🧩 خطة نسخة القالب (Template) — مطلوبة، مش متنفذة

**الهدف:** نسخة نضيفة من كل الملفات، كل قيمة خاصة بمدينة مستبدلة بعلامة واضحة، بحيث المدرسة الجديدة تملى العلامات وخلاص.

### أ) شكل العلامة
```
{{SCHOOL_NAME}}          {{FIREBASE_API_KEY}}
{{SCHOOL_EMAIL}}         {{FIREBASE_PROJECT_ID}}
{{SCHOOL_ADDRESS}}       {{FIREBASE_APP_ID}}
{{DOMAIN}}               {{FIREBASE_SENDER_ID}}
{{OWNER_EMAIL}}          {{RECAPTCHA_SITE_KEY}}
{{GH_OWNER}}             {{GH_REPO}}
{{VIPPS_MSN}}            {{BANK_ACCOUNT}}
{{PRICE_MAIN}}           {{PRICE_KIDS}}
{{SIBLING_DISCOUNT_2}}   {{SIBLING_DISCOUNT_3}}
{{WEB3FORMS_KEY}}        {{BACKUP_EMAIL}}
```

📌 صيغة `{{...}}` مقصودة: سهلة تتلاقى بالبحث، ومستحيل تشتغل بالغلط لو حد نسي يملاها — الصفحة هتكسر بصوت عالي بدل ما تشتغل غلط في صمت.

### ب) الترتيب المقترح
1. **`config.js` الأول** — ملف واحد يجمع كل القيم، وكل الصفحات تقرا منه بدل ما القيم تتكرر في 7 ملفات
2. `functions/index.js` — أقل ملف فيه نصوص واجهة
3. `publish-tool.html` — أصغر وأسهل اختبار
4. البوابات: `index1` · `index4` · `index2`
5. `admin.html` — الأكبر والأصعب، آخر حاجة
6. الصفحات العامة: `index` · `index3` · `timeplan` · `kunngjoring` · `personvern` · `madina-brosjyre`

### ج) العقبة الحقيقية — النصوص
النصوص النرويجية/العربية/الأردية مكتوبة **جوه الكود** في كائنات `I18N` و `TEXT` ضخمة. دول **مش قيم إعدادات** — دول محتوى.

خياران:
- **الأسهل:** تسيب النصوص زي ما هي، وتستبدل اسم المدرسة والعنوان بس. المدرسة الجديدة تعدّل النصوص بنفسها
- **الأنضف:** تفصل كل `I18N` في ملف `texts.js` منفصل. شغل أكبر بكتير، بس النتيجة قالب حقيقي

### د) ملف `SETUP.md` يتعمل مع القالب
خطوات الإعداد بالترتيب: إنشاء Firebase → الدومين → Brevo → القواعد → App Check → البيانات الأولية → أول نشر.

### هـ) ⚠️ تحذيرات لازم تتنقل مع القالب
- **متكرّرش إعداد App Check الحالي** — هو مكسور هنا (0% توكنات صالحة). القالب لازم يا يشيله يا يوثّق الطريقة الصح
- **`onSnapshot` لازم يمر على `watch()`** — الغلطة دي اتكررت 4 مرات
- **`us-central1`** — القالب يبدأ بـ`europe-west1` لو المدرسة أوروبية
- **رقم الحساب البنكي placeholder** — لازم يبقى `{{BANK_ACCOUNT}}` مش رقم وهمي، عشان ما يتبعتش لأهالي بالغلط

---

## 24) ⚙️ إخراج القيم من الكود لمجموعة `settings` — ✅ المرحلة الأولى اتنفذت (شوف 29)

**المشكلة اللي أثبتت الحاجة:** خصم الإخوة مكتوب في **4 أماكن** (`admin.html` · `functions/index.js` · البروشور · `rabattforslag.html`) — وطلعوا **مختلفين**. أي قيمة متكررة في أكتر من مكان هتتفرّق عاجلًا أو آجلًا.

**الهدف:** أي حاجة الإدارة ممكن تحب تغيّرها تتدخل من اللوحة، مش من الكود.

### أ) القيم اللي تخرج

| القيمة | فين دلوقتي |
|---|---|
| نسب خصم الإخوة | `admin.html` + `functions/index.js` |
| `PROGRAM_PRICES` | نفس المكانين (والأنضف: تتقرا من `programs`) |
| **سعر العضو لكل برنامج** | مش موجود — حقل جديد |
| `INVOICE_BANK_PLACEHOLDER` | `admin.html` |
| `MAIL_FROM` / `BACKUP_EMAIL` | `admin.html` + `functions/index.js` |
| `INACTIVITY_LIMIT_MS` (20 دقيقة) | `admin.html` + `index1.html` |
| أجر الساعة (200 معلم / 150 مساعد) | `admin.html` + `index1.html` |
| `AUTO_CLOSE_DAYS` (14 يوم) | `index1.html` |
| `CONFIRM_LOCK_DAYS` (30 يوم) | `slettelogg.html` |
| `MAX_FILE_MB` (10) | 4 ملفات |
| `KEEP_DAYS` (60 يوم) | `functions/index.js` |

### ب) 🔴 اللي **لازم يفضل** في الكود
- **الأسرار** (مفاتيح فيبس) → Secret Manager
- **الأدوار والصلاحيات** (`OPPTAK_ROLLER` · `ADMIN_PANEL_ROLES` · `SENIOR_LEADERSHIP_ROLES`)

📌 **السبب:** لو الأدوار اتنقلت لقاعدة البيانات، أي حد يقدر يكتب فيها يقدر **يدّي نفسه صلاحية**. القواعد الحالية بتسمح لأي حساب مسجّل يكتب في أي مجموعة — يعني ده هيبقى ثغرة تصعيد صلاحيات مباشرة.

### ج) التنفيذ
1. توسيع مجموعة `settings` (موجودة أصلًا لرابط فيبس)
2. تبويب **⚙️ Innstillinger** في اللوحة
3. `functions/index.js` يقرا من `settings` بدل الثوابت
4. البوابات تقرا من `settings` عبر `watch()`

⚠️ **لازم يكون فيه قيم افتراضية في الكود** — لو `settings` فاضية أو القراءة فشلت، النظام يشتغل بالقيم الحالية بدل ما يقع.

### د) الترتيب المقترح
1. الأسعار وخصم الإخوة (أكتر حاجة اتلخبطت)
2. رقم الحساب والإيميلات
3. المدد الزمنية والأجور
4. تبويب الإعدادات

---

## 25) 💰 مذكرة سعر العضوية (8 أغسطس) — «Beregnet, ikke vedtatt»

ملف `medlemspris-forslag-2026-2027.html`.

### الخلاصة
مقترح Usman («500 kr لكل طفل شهريًا») **ما ينفعش يتحوّل لنسبة مئوية**:
- `1000 ÷ 5500 = 18,1818…%` — عدد دوري
- مدينابارن (3 000/سنة) كان هيبقى **أغلى** للعضو (4 500) — العضوية تبقى عقوبة
- الترم كان هينقسم لمبلغين مختلفين (2 000 و 2 500)

### البديل المقترح: سعر عضو ثابت مخزّن على كل برنامج

| البرنامج | شهري | سنوي (9 شهور) | ترم | السعر الكامل |
|---|---|---|---|---|
| البرنامج الأساسي (السبت) | 500 | 4 500 | 2 250 | **5 500** |
| مدينابارن (الأحد) | 250 | 2 250 | 1 125 | **3 000** |

### 🔴 ده بيغيّر أرقام في الكود
| القيمة | الكود دلوقتي | حسب المذكرة |
|---|---|---|
| خصم الإخوة | **20% / 30%** | **10% / 20%** |
| سعر مدينابارن | 1 500 (ترم) | **3 000/سنة** |
| سعر العضو | مش موجود | حقل جديد في `programs` |

### القواعد
- خصم العضوية وخصم الإخوة **لا يجتمعان أبدًا**
- العضو: نفس السعر لكل طفل، **مفيش خصم إخوة**
- غير العضو: سعر كامل + خصم إخوة

### ⚠️ نقطة مفتوحة — الخصم يتحط على أنهي برنامج؟
المذكرة بتقول **الأرخص**. المالك يفضّل **الأغلى**.

مثال (طفل في الأساسي + طفل في مدينابارن):
| الاختيار | إجمالي العيلة/سنة |
|---|---|
| على الأرخص | 8 200 kr |
| على الأغلى | **7 950 kr** |

الفرق **250 kr في السنة** للعيلة. لـ20 عيلة = 5 000 kr سنويًا على المدرسة.

📌 **الأغلى أنضف منطقيًا** («الخصم على أكبر مبلغ») وأسهل يتشرح لولي أمر. الفرق المالي بسيط. **لكن المذكرة مكتوب فيها «الأرخص» — لازم تتعدّل قبل ما تتعرض على الإدارة.**

### 🔴 متغيّرش الكود قبل الاعتماد
الملف مكتوب فيه **«Beregnet, ikke vedtatt»**. الأسعار الحالية بتتبعت لأولياء أمور حقيقيين — تغييرها على أساس مقترح ممكن يترفض هيلخبط الفواتير.

---

## 26) 🛡️ حماية النشر من النسخ القديمة

**المشكلة:** الأداة كانت بتجيب الملف من GitHub قبل كل رفعة عشان `sha` بس، وترمي المحتوى.

### أ) النتايج السبعة

| النتيجة | الشرط | التصرف |
|---|---|---|
| 🆕 `ny` · ✓ `nyere` · ✓ `endret` · ✓ `ukjentNy` | | ✅ اسمح |
| ⛔ `identisk` | نفس المحتوى | 🛑 منع |
| 🛑 `eldre` | ختمك أقدم | 🛑 منع |
| 🛑 `historisk` | المحتوى = نسخة قديمة في التاريخ | 🛑 منع |

📌 **`endret` أهم صف:** تنزّل ملف وتعدّله → الختم بيفضل زي ما هو. لو اتحسبت تضارب، الأداة كانت هتقفل على كل تعديل عادي.

### ب) تلات طبقات

1. **الختم** — فوري
2. **السجل المحلي** (`localStorage`، آخر 60 لكل ملف)
3. **تاريخ GitHub** — آخر 30 نسخة

📌 **الحتة اللي بتخليه فوري:** الأداة بتقارن آخر حاجة في السجل بالمنشور فعلًا — مقارنة **مجانية** لأنها جابت الملف أصلًا. اتطابقوا → السجل موثوق كامل → الطبقة التالتة ما بتشتغلش. **20 رفعة = 32 طلب للأولى، وطلب واحد لكل اللي بعدها.**

### ج) الأساس المفقود (مطلوب، مش متنفذ)

الختم بيتحقن في النسخة الرايحة لـGitHub بس. **الحل الجذري: الختم يتولّد وقت التعديل مش وقت النشر.** الاسم `SIST-ENDRET` بيقول «آخر تعديل» وبيسجّل «وقت النشر».

⚠️ **أثر جانبي معروف:** أي ملف يتسلّم من محادثة بيورّث ختم النسخة الأصلية → الحارس بيقراه «أقدم» ويمنع شغل صحيح. **الحل: شيل الختم من أي ملف قبل تسليمه.**

### د) حاجات تانية في الأداة

- **📜 اللوج** — بيمشي على تاريخ الملف. أحمر = بيقولك عند أي نسخة الخلل بدأ
- **⬇️ تنزيل / 📋 نسخ** — النسخة المنشورة أو أي نسخة قديمة (إنقاذ بعد رجوع للخلف)
- **📦 Hele prosjektet** — ZIP بكل الملفات + الملخص. مولّد ZIP من الصفر بدون مكتبات
- **بانر النسخة القديمة** — الأداة بتقارن الختم في **الصفحة الشغالة فعلًا** (كومنت الـDOM) بالمنشور، وبتحذّر لو التاب قديم
- **📋 Kopier meldingen** — تحت أي صندوق حالة فيه رسالة، بيضيف اسم الملف والوقت

⚠️ **الأرشيف بيستبعد الملفات المخفية** — `.env.madina-instituttet` و `.firebaserc` **مش جواه**

⚠️ **`index.html` في 3 أماكن** والأداة بتختار الجذر. رفع `index.html` بتاع مجلد فرعي بالاسم **بيدوس على الصفحة الرئيسية**

---

## 27) 🔴 الباگ اللي خصى نظام الأختام

`stemplFil` بتشيل الختم القديم بـ regex. ولما الملف المنشور هو **الأداة نفسها**، الـregex بيلاقي القالب المكتوب جوه الكود ويمسحه → `const merke = '';`

**النتيجة:** كل ملف HTML/MD اتنشر من 7 أغسطس 23:14 طلع بلا ختم، وبسطر فاضي بيتراكم.

**الإصلاح:** `MERKE_START = '<' + '!--'` — النص مابقاش موجود كسلسلة متصلة.

📌 **الدرس:** أي أداة بتعالج ملفات وبتنشر نفسها لازم تتسأل: **«لو اشتغلت على كودها هي، هتعمل إيه؟»**

**أول ختم صحيح: 8 أغسطس 15:22:39.**

---

## 28) ✅ App Check — اتحل

**السبب:** مفتاح reCAPTCHA كان مسجّل عليه `madina.no` بس — والموقع على `madinaskole.no`. دومين مختلف تمامًا.

**الإصلاح:** `google.com/recaptcha/admin` → ⚙️ Settings → Domains → ضيف `madinaskole.no` و `madinaskolen.no`.

### الأرقام قبل الإصلاح (Aug 1–9، مرجع)
| النوع | النسبة |
|---|---|
| Verified | **0%** (0/758) |
| Invalid | 93% |
| Outdated client | 4% |
| Unknown origin | 3% |

### 🔴 لسه ممنوع Enforced
الـ7% دول **مش هيختفوا** — متصفحات بتحجب reCAPTCHA وأجهزة قديمة. **لو اتفعّل الفرض، الناس دي بتتقفل بره.** الشرط: فوق 90% ثابت كام يوم، بنطاق **24 ساعة** مش 7 أيام.

### 🧪 `appcheck-test.html`
بتفصل المشكلة لـ3 خطوات وبتوقف عند أول فشل مع كود الخطأ وسببه بالنص.

⚠️ **Functions مش محمية بـApp Check خالص.** · ⚠️ متدوسش «reCAPTCHA Enterprise» — الكود v3.

---

## 29) ⚙️ مجموعة `settings`

### `settings/priser`
```
soskenRabatt2: 20 · soskenRabatt3: 30 · tellForesatt2: false
```
**تتعدّل من:** Økonomi → Betalingsinnstillinger → كارت Søskenrabatt، مع معاينة حية بالكرونة.

🔴 **العطل الحي اللي اتصلح:** `admin.html` كان بيقرا السعر من `programs`، و`functions/index.js` من ثوابته. **تغيير سعر من اللوحة كان بيخلي فيبس يخصم رقم تاني.**

⚠️ **`tellForesatt2` = false** — الطفل تحت ولي الأمر التاني مش بيتحسب كأخ **والعيلة بتدفع أكتر**.

### `settings/varsler`
```
nySoknadAktiv · kvitteringAktiv · nySoknadMottakere[] · varselInitiert
```
📌 المستقبلين **إيميلات مش IDs** — عشان الإشعار يفضل شغال لو الكارت اتمسح واتعمل تاني.

### `settings/helse`
```
varselSisteKjoring · sikkerhetskopiSiste · varselFastSiden · sisteSjekk · sisteHelseVarsel
```

### 🔴 اللي لازم يفضل في الكود
**الأدوار والصلاحيات** (`OPPRETT_ROLLER` · `ADMIN_PANEL_ROLES`). القواعد بتسمح لأي حساب مسجّل يكتب في أي مجموعة — لو الأدوار اتنقلت للقاعدة، **أي معلم يدّي نفسه دور Eier**.

---

## 30) 📬 نظام الإشعارات

### 🔴 المحاولة الفاشلة
اتكتبت أول مرة `onDocumentCreated`. **النشر فشل:**
```
Location europe-north2 is not found or access is unauthorized
```
⚠️ والفشل كان **غير مرئي في GitHub Actions** — نشر Hosting نجح على نفس الـcommit فالقايمة بانت خضرا.

### الحل: `onSchedule` كل دقيقتين
1. تجيب `registrations` وتفلتر `varselSendt !== true`
2. **أول تشغيلة:** تعلّم القديم كله **من غير إرسال** (حارس `varselInitiert`)
3. كل طلب جديد → إشعار للإدارة + إيصال لولي الأمر → تعليم
4. فشل الإرسال → الطلب يفضل غير معلّم ويتجرّب تاني

📌 التعليم **بعد** ما الإيميل يتحط في الطابور. إشعار متكرر أهون من ضايع.

### 🔧 طريقة نشر الدوال
```
cd المسار → cd functions → npm install → cd .. → firebase deploy --only functions
```
⚠️ **تلات فخاخ:** `npm install` إجباري · `.env.madina-instituttet` مش في الأرشيف · Notepad بيزوّد `.txt`

🔴 **متستخدمش `.env.example`** — فيه `SITE_BASE_URL` بتاع GitHub Pages الميت، بيودّي أولياء الأمور لصفحة 404 **بعد الدفع**.

📌 **GitHub Action بتاعة الدوال اتصلحت** بعد التحويل لـ`onSchedule` — النشر من الأداة كافي دلوقتي.

### Web3Forms اتشال
المفتاح كان مكشوف في صفحة عامة، والمستقبل ثابت، وبيانات الأطفال بتعدّي على طرف تالت. **اتشال نهائيًا.**

---

## 31) 🩺 تحويل الفشل الصامت لمرئي — تلات مستويات

### المستوى 1: صفحات تشخيص
- **`appcheck-test.html`** — App Check
- **`helsesjekk.html`** — 6 فحوصات + حكم واحد فوق + **📋 Kopier rapport**

### المستوى 2: أثر في القاعدة
- `varselSendt` على كل طلب — **الغياب نفسه دليل**
- نبض (`varselSisteKjoring` · `sikkerhetskopiSiste`) — **جوب مش بيشتغل مابيقولش، بيسكت بس**
- **مجموعة `feillogg`** — الأهم

🔴 **`feillogg`:** فيه **112 مسار خطأ صامت** في `admin` و`index1` و`index4` (`catch → console.error`). على الموبايل مفيش console. بدل تعديل 112 مكان، الكود **بيعترض `console.error` نفسه** — كل `catch` موجود بقى مرئي، وأي `catch` جديد بيتغطى تلقائيًا.

**أربع حمايات:** قفل ضد اللوب · 25 لكل جلسة · نفس الخطأ = صف واحد (10 دقايق) · مابيكتبش قبل الدخول.

⚠️ محتاجة قاعدة في `firestore.rules` **قبل** الـcatch-all:
```
match /feillogg/{id} {
  allow create: if request.auth != null;
  allow read: if request.auth != null;
}
```

### المستوى 3: تنبيه ذاتي
الدالة المجدولة بتفحص كل نص ساعة وبتبعت إيميل **بس لما فيه حاجة غلط**:
- النسخة الاحتياطية عدّى عليها 8 أيام
- إيميلات فاشلة **في آخر أسبوع** (النافذة أسبوع عشان تستنى لو مشغول)
- إشعار واقف أكتر من ربع ساعة
- أخطاء في `feillogg` آخر أسبوع

⚠️ **إيميل واحد كل 24 ساعة كحد أقصى.** تنبيه بييجي كل يوم بيتتجاهل بعد أسبوع.

🔴 **فخ اتمسك في الاختبار:** `Date.parse(0)` بيقرا `"0"` كسنة 2000 — **تاريخ صحيح**. `Date.parse(felt || 0) || 0` كان هيخلي أي حقل ناقص = سنة 2000، وأول تشغيلة كانت هتبعت «النسخة الاحتياطية عمرها 9000 يوم». الحل: دالة `tidspunkt()`.

---

## 32) 🗂️ إعادة بناء اللوحة

### أ) المجموعات — مرتبة حسب **مين صاحب القرار**

| المجموعة | التبويبات |
|---|---|
| 🏛️ Eier / Ledelsen | Ledelse · Møtereferater · Meldingssenter |
| 💰 **Økonomi** (جديدة) | Kasse · Lønn · Betalingslogg · Kasserer · Betalingsforespørsler · Betalingsinnstillinger |
| 🗂️ Administrativ | Lærere · Foresatte · Elever · Kontaktendringer · Rapporter |
| 🎓 Pedagogisk | Klasser · Timeplan · Oppmøte · Fravær · Forberedelser · Planer · Lekser · Kunngjøringer · Elevnotater · Meldinger |
| 🏫 **Skoleoppsett** (جديدة، Eier) | Programmer · Skolekalender |
| 📝 Registrering | Søknader |
| ⚙️ Oversikt | Oversikt (6 مجلدات) |
| 🛠️ **Verktøy** (جديدة، Eier) | Verktøy og sider (6 مجلدات، 24 رابط) |
| 📖 **Veiledning** (جديدة) | الكتالوج |

🔴 **التداخل اللي اتحل:** الفلوس كانت متفرقة على مجموعتين (6 تبويبات). `Programmer`/`Skolekalender` كانوا إداري رغم إنهم Eier. `Klasser`/`Timeplan` كانوا إداري وهما قرار تربوي.

### ب) الصلاحيات — `OPPRETT_ROLLER` في `admin.html`

| القسم | مين |
|---|---|
| Programmer · Skolekalender | **Eier** |
| Klasser · Timeplan | Eier + **Faglig / pedagogisk leder** |

📌 القواعد في **مكان واحد**، والزرار مابيختفيش في صمت — مكانه بيظهر «Kun Eier kan legge til her».

⚠️ **سبر في المتصفح، مش قاعدة أمان.**

### ج) الإضافة في مكان واحد
كل الأشخاص (معلمين · أولياء · طلاب) بيتضافوا من **Oversikt → Ansatte og tilgang**. اتشالت من 4 تبويبات، ومكانها سطر بيوديك على المكان الجديد.

📌 **التعديل سابته في كارت الشخص** — الإضافة مالهاش سياق فمكان ثابت أحسن، والتعديل ليه سياق (الشخص اللي فاتحه).

### د) الكروت بقت جدول
خطوط رأسية بين الخانات وأفقية بين الصفوف، `--tabell-linje: rgba(40,40,90,0.26)`. على الموبايل الخطوط بتتقلب أفقية. **21 قائمة** بتستخدم نفس `.record-card`.

⚠️ الرفع عند اللمس اتشال (كان بيكسر الشبكة) واتبدل بتظليل.

### هـ) التصدير — نافذة واحدة لـ18 تبويب
اختيار **أعمدة** + **صفوف** + **CSV / طباعة-PDF / بريد**.

🔴 **السبب مش الراحة:** `students` عنده **58 عمود**. قايمة أسماء كانت بتتطلب تنزيل كل حاجة — **والملف اللي بيتبعت فيه بيانات صحية وموافقات**.

📌 PDF بيستخدم نافذة طباعة المتصفح بدل مكتبة. البريد بينزّل الملف ويفتح رسالة — **البيانات مش بتتحط في الرابط**.

### و) 📖 Veiledning — كتالوج الاستخدام
10 مواضيع: بنية اللوحة · الصلاحيات · إضافة برنامج · الأسعار · التقويم · الأشخاص · التصدير · الإشعارات · لما حاجة ما تشتغلش · اللي مش بيتغير من اللوحة.

⚠️ **متاح لكل حد عنده دخول، مش للمالك بس.**

📌 **قاعدة مكتوبة في الكود:** أي تغيير في اللوحة، النص هنا يتحدّث في نفس الشغلة.

---

## 33) 👤 إكمال بيانات المعلم عند أول دخول

```
الأدمن: الاسم + الإيميل → إيميل تعيين باسورد
→ أول دخول: شاشة إكمال ← مايعديش من غيرها
→ البوابة تفتح، والكارت اتملى لوحده
```

**الحقول:** تليفون · المادة · الجنس · حالة الأجر · رقم الحساب.

⚠️ **الفرّيلي مش بيتطلب منه رقم حساب** · ⚠️ **`tildeltKlasse` مش في القايمة** — قرار تربوي بتاخده المدرسة.

📌 **المعلم اللي بياناته كاملة مش بيحس بأي تغيير.**

🔴 **باگ اتمسك بعد النشر:** دالة التحقق كانت بتقرا من **النموذج بس**. الحقول المليانة من قبل مش بتترسم → اتحسبت ناقصة → «Gjenstår: fag» من غير خانة، والزرار مقفول للأبد. **الحل:** التحقق بيبدأ من كارت المعلم وبيحط النموذج فوقه.

---

## 34) 🏛️ `ledelseinnsyn.html` + تسجيل الجلسات

⚠️ **اللوحة مكانتش بتسجّل الدخول أصلًا.** البوابات بتكتب في `loginEvents`، الأدمن لأ.

**إزاي بتتقاس الدقايق:** المتصفح عمره ما بيقول إن التاب اتقفل. الجلسة بتتحدّث بنبضة **كل دقيقتين وإنت شغال** — المدة = من أول نبضة لآخر واحدة. النبضة معلّقة على الحركة والكتابة، **فالتاب المفتوح من غير استخدام مابيتحسبش**.

**الصفحة بتعرض:** جلسات ودقايق (7 و 30 يوم) · متوسط الجلسة · **«مادخلش ولا مرة»** · «ساكت من X يوم» · عمليات الحذف (30 يوم) · ملخص فوق · 📋 نسخ التقرير.

⚠️ جلسات المعلمين **مش بتتخلط** مع جلسات اللوحة (فلترة على `role: 'admin'`).

📌 البيانات بتبدأ من أول دخول بعد الرفع — اللي فات مش موجود.

---

## 35) 📝 سجل التعديلات على الأقسام القديمة

**التعديلات دي اتطبقت في الملف ده فعلًا** — القسم موجود عشان تعرف إيه اللي اتغير، مش عشان تنفذه.

| القسم | اللي حصل |
|---|---|
| **0** | اتعاد بناؤه — 4 حقائق دائمة + تحذيرات حية + نمط الفشل الصامت |
| **2** | بقى «حالة الملفات» بدل جدول التوقيتات — الختم جوه الملف هو المرجع |
| **5** | App Check اتحل → التفاصيل في 28 |
| **16** | اتقسم لقرارات / شغل تقني / تنضيف. خلص 5 بنود |
| **18** | اتعاد تنظيمه لـ4 مجموعات، من 8 لـ20 ملاحظة |
| **24** | المرحلة الأولى اتنفذت → 29 |
| **26** | كان «مطلوب مش متنفذ» → اتنفذ بالكامل |

### 🔴 أعطال اتكشفت وما كانتش في الملخص أصلًا

| العطل | الخطورة |
|---|---|
| `foreldreinnsyn` و `larerinnsyn` بيقبلوا **أي حساب** رغم «kun for eier» | أي معلم يشوف نشاط زمايله، أي ولي أمر يشوف كل الأهالي |
| `admin.html` كان بيقرا السعر من `programs` والدوال من ثوابتها | تغيير سعر من اللوحة = فيبس يخصم رقم تاني |
| فيبس: الطلب بيفضل «منتظر الدفع» بعد الدفع | «Kasserer» بيعرض إلغاء طلب مدفوع |
| مستمع `settings/betaling` قبل الدخول | خانة رابط فيبس بتفضل فاضية مهما كان المحفوظ |
| اللوحة مكانتش بتسجّل الدخول أصلًا | مفيش أي بيانات عن استخدام اللوحة |
| **32 مستمع بيموتوا في كل تسجيل دخول** | «مش متصل» + refresh «بيصلحها» |

### 📌 نفس النمط في 3 أماكن مختلفة — «الشرط القريب»
- زرار «Vis mer» بيسأل «فيه حقول أساسية؟» بدل «فيه حقول مخفية؟» → زرار مالوش وظيفة
- شاشة إكمال بيانات المعلم بتقرا من النموذج بدل الكارت → بتطلب حقل مفيش خانة ليه
- عدّاد القوائم بيقرا من جدول ناقص → «2 undefined»

---

## 36) 🗓️ اللي حصل يوم 8 أغسطس

| # | الحدث |
|---|---|
| 1 | `foreldreinnsyn` + `larerinnsyn` اتصلحوا (Safari + `watch()` + ثغرة صلاحيات) |
| 2 | حماية النشر — 3 طبقات + لوج + تنزيل/نسخ + ZIP + بانر النسخة القديمة |
| 3 | أداة النشر كانت بتخصي نفسها — اتكشف واتصلح |
| 4 | **App Check اتحل** — الدومين |
| 5 | عطل Safari اتقفل في 8 ملفات |
| 6 | الأسعار اتوحّدت بين اللوحة وفيبس |
| 7 | الإشعارات + إيصال ولي الأمر — بعد اكتشاف `europe-north2` |
| 8 | مفتاح Web3Forms المكشوف اتشال |
| 9 | `feillogg` — 112 مسار صامت بقوا مرئيين |
| 10 | تنبيه ذاتي + `helsesjekk.html` |
| 11 | باگ فيبس: «منتظر الدفع» بعد الدفع |
| 12 | إعادة بناء التبويبات + الصلاحيات + الكروت الجدولية |
| 13 | نظام التصدير (أعمدة + صفوف + 3 صيغ) |
| 14 | كتالوج الاستخدام |
| 15 | إكمال بيانات المعلم عند أول دخول |
| 16 | `ledelseinnsyn.html` + تسجيل الجلسات |
| 17 | إصلاح الرجوع والتحديث في اللوحة |

**دروس اليوم:**

- **الدليل من الجهاز حسم كل حاجة.** App Check اتحدد من رسالة حمرا صغيرة في ركن الشاشة. وفشل النشر ما بانش غير من `firebase deploy` على الجهاز.
- **فحص الـsyntax مسك ملفات مكسورة 3 مرات.** الإضافات ابتلعت سطر تعريف دالة.
- **الاختبار مسك باگات كانت هتتسلّم:** `Date.parse(0)` · `undefined` في السجل · دالة خروج غير موجودة.
- **مرة فرّغت ملف لصفر بايت** — `open(w)` بيمسح قبل ما يكتب، والكتابة فشلت. من ساعتها: اكتب في ملف مؤقت وتأكد من حجمه قبل الاستبدال.
- **متكتبش ملف كامل وإنت مش شايف آخر نسخة منه.**

---

## 37) 💰 نظام التسعير — نموذجين (9 أغسطس)

### أ) النموذجان

| | العضو | غير العضو |
|---|---|---|
| الأساس | سعر **شهري** ثابت × عدد الشهور | سعر **سنوي** |
| خصم الإخوة | **لأ** — نهائيًا | أيوه |

**الأرقام الحالية:** البرنامج الرئيسي 500/شهر أو 5500/سنة · Madinabarn 250/شهر أو 3000/سنة · 9 شهور (4 خريف + 5 ربيع) · رسوم مواد 450 · وديعة 500 خلال 5 أيام.

📌 **ولا رقم منهم في الكود.** كلهم في `programs` و `settings/priser`.

### ب) خصم الإخوة على البرنامج **الأعلى** سعرًا

الترتيب تصاعدي: الأرخص سعر كامل، والخصم بيقع على الأغلى — فالعيلة توفّر أكتر.

⚠️ **وعند تساوي الأسعار، الترتيب بيتحسم بالـid** — عشان اللوحة وفيبس يوزّعوا نفس التوزيع بالظبط.

### 🔴 ج) ليه خصم الطفل التالت 15% مش 20%

مع 20%: الطفل التالت لغير العضو = **4400**، وسعر العضو = **4500**.

**يعني العضوية بتبقى أقل فايدة لأصحاب العيال الكتير** — عكس المقصود.

```
15% → 4675   ✓ العضوية دايمًا أوفر
18% → 4510   ✓ بهامش 10 كرونة بس
20% → 4400   🔴 القاعدة بتنكسر
```

📌 **السقف 18%.** فوقه الجملة المكتوبة في الاستمارة («سعر العضوية دايمًا الأقل») بتبقى كذب.

⚠️ **الدرس:** الخصمين **مش مستقلين**. تغيير واحد منهم لازم يتفحص ضد التاني.

### د) العضوية مش خانة في الاستمارة

ولي الأمر **مايعلنش** إنه عضو — يكتبها في الملاحظات والإدارة بتتحقق. الخانة في **كارت الطالب** (`erMedlem`).

⚠️ **الخلط ممنوع منطقيًا:** لو إخوة بحالة عضوية مختلفة، اللوحة بتحذّر — الحساب ساعتها مبني على افتراض مكسور.

### هـ) الشهور على **البرنامج** مش على المدرسة

`programs.antallManeder` — عشان برنامج صيفي 3 شهور يشتغل جنب برنامج 9 شهور. **اتختبر: 400×3+450 = 1650 جنب 500×9+450 = 4950 في نفس العيلة.**

### و) الاستمارة بقت تقرا من القاعدة

كل رقم فيها (2750/ترم · 500/شهر · «9 måneder» · «Du sparer 1000 kr» · الخصومات · الوديعة · المواد) بيتقرا لحظيًا.

📌 **السعر بيظهر جوه البرنامج المختار**، مش قايمة منفصلة.

🔴 **لو القراءة فشلت، مفيش أرقام بتتعرض** — «Prisene oppgis av administrasjonen». **سعر قديم على استمارة تسجيل هو وعد المدرسة مش هتقدر تنفذه.**

⚠️ **ده احتاج فتح `programs` و `settings/priser` للقراءة العامة.** مفيش بيانات أشخاص فيهم، وكلهم بيتعرضوا على الاستمارة أصلًا.

---

## 38) 💸 التكلفة — 79% من الحصة المجانية في يوم واحد

🔴 **الكود اللي كتبته يوم 8 أغسطس كان بيستهلك 79% من حصة القراءة المجانية، ومحدش نبّه.**

### السبب: سطر واحد

`varsleNySoknad` كانت بتقرا **كل الطلبات** كل دقيقتين عشان تدوّر على واحد جديد.

```
20 طلب  →  14 400 قراءة/يوم
50 طلب  →  36 000
200 طلب → 144 000   ← 3× الحصة
500 طلب → 360 000   ← 720%
```

**وبتكبر مع كل طلب جديد.**

### الإصلاح

| | قبل | بعد |
|---|---|---|
| قراءات/يوم | 39 600 (79%) | **903 (1.8%)** |
| كتابات/يوم | 2 928 (15%) | 1 596 (8%) |

1. **استعلام بدل قراءة الكل** — `where('varselSendt','==',false)`. مفيش جديد = صفر قراءة، **مهما كبر العدد**
2. **الفحص كل ساعتين** بدل كل نص ساعة
3. **نبضة الجلسة كل 5 دقايق** بدل دقيقتين

### ⚠️ التبعية اللي لازم تتعرف

Firestore **مايقدرش يسأل عن «حقل ناقص»**. فالاستمارة لازم تكتب `varselSendt: false` صراحةً وقت التسجيل.

🔴 **لو `index2.html` مش مرفوع مع الدوال، الطلبات الجديدة مش هيتبلّغ عنها إطلاقًا** — لأن الاستعلام مش هيلاقيها.

📌 **والنمط ده أخطر من الفشل الصامت العادي:** مفيش حاجة بتتكسر، والأرقام بتبان معقولة — **لحد ما تيجي فاتورة Google آخر الشهر.**

---

## 39) 🔑 الأدوار والصلاحيات كبيانات (9 أغسطس)

### أ) اللي اتشال من الكود

`ADMIN_PANEL_ROLES` · `SENIOR_LEADERSHIP_ROLES` · `OPPRETT_ROLLER` · `ROLLE_NIVA` — **الأربعة بقوا صفر.**

بقوا مجموعة **`roller`**، والمالك بيحررها من تبويب **🔑 Roller og tilganger**.

**كل دور فيه:** نيفو (1-5) · دخول اللوحة · يُحسب إدارة · معالجة الطلبات · إيه اللي يقدر ينشئه.

### ب) التسلسل

```
1  Eier
2  Skoleleder · Daglig leder
3  Faglig leder · Fagkoordinator · Opptaksansvarlig
4  المعلمين
5  Assistent · Frivillig · Annet
```

**القاعدة:** تعيّن **أقل منك بدرجة على الأقل**. المالك وحده يعيّن مالك. **ولا يحذف المالك إلا المالك.**

🔴 **مفروض في `firestore.rules`، مش في الواجهة.** الواجهة بتخفي الأزرار وتشرح — **القواعد هي اللي بتمنع**. اللي بيفتح الكونسول بيقابل القواعد.

### ج) 🔴 مفتاح الطوارئ — متشيلوش أبدًا

```
function erEier() {
  return ... request.auth.token.email.lower() == 'yasser.m.s.abdelrahim@gmail.com';
}
```

المالك بيدخل **مهما حصل للقاعدة**: اتمسحت · اتكتبت غلط · مالك معيّن شال الأصلي.

📌 **الإيميل هناك مش بيدي صلاحية لوحده** — الباسورد في Firebase Auth.

### د) 🔴 الدورة المقفولة — عطل تسبب في قفل المالك بره

**صممت «الجدول فاضي = مفيش صلاحية» على إنه الأأمن. ودي قفلت الباب على نفسها:**

```
فحص الصلاحية  ←  محتاج جدول roller
جدول roller   ←  مابيتحمّلش غير بعد نجاح الفحص
```

**النتيجة: المالك اتقفل بره لوحته.**

**الإصلاح:** الجدول بيتقري **قبل** الفحص · ولو فاضي، بيستخدم `ROLLER_UTGANGSPUNKT` (نفس الإعدادات المحافظة اللي كانت في الكود).

⚠️ **الدرس:** «الافتراض هو المنع» صح كمبدأ أمان — **بس مش لما النظام محتاج نفسه عشان يشتغل**. أي منظومة صلاحيات محتاجة نقطة انطلاق بره نفسها.

### هـ) ترتيب الرفع

🔴 **`admin.html` الأول، وبعد الدخول القواعد.** لو اتعكس، `roller` مش هتتبني والقواعد هترفض إنشاءها.

---

## 40) ⚠️ واقعة 9 أغسطس 06:46 — تعديل مجهول المصدر

**ملفان في مساحة العمل اتعدّلوا قبل أول أمر في المحادثة بدقيقتين ونص:**

```
06:46:00  functions/index.js
06:47:04  admin.html
06:49:23  ← أول أمر من Claude
```

**المحتوى:** تنفيذ كامل لنموذج التسعير اللي اتقال ليلتها ورُفض تنفيذه لحد الصبح.

### اللي اتفحص

| الاحتمال | النتيجة |
|---|---|
| من المحادثة | ❌ صفر أثر في 1.8 ميجا |
| من المالك | ❌ كان نايم |
| نشر على GitHub | ❌ آخر commit 01:57 |
| محادثة أقدم | ❌ ما كانش عندها الملف الحالي |
| مزامنة/استعادة | ❌ **فجوة 64 ثانية** — الأوامر الجماعية بتكتب في نفس الثانية |

🔴 **الدليل الحاسم:** الملف مبني على النسخة المنشورة **01:57 نفس الليلة** — وفيه دالة `noeFeilet` اتكتبت الساعة ~01:30 ردًا على خطأ اتلقى في نفس المحادثة. **مفيش حاجة بره المحادثة كان عندها الكود ده.**

### المحتوى

533 كيلو اتقارنت سطر بسطر: **23 كتلة، 22 تسعير، والأخيرة سطر فاضي.** إعدادات Firebase · روابط فيبس · قايمة المالك · الصلاحيات · النداءات الخارجية — **كلها بدون تغيير.** والـ293 سطر المضافين بيلمسوا `document` بس.

### القرار

**اتعمل من الأول على النسخة المنشورة** — مش لأن فيه خطر تقني، لكن لأن ملف بيحدد فلوس عيلات وصلاحيات موظفين **مايستاهلش تفضل شاكك فيه**.

📌 **البيئة مابتسجّلش هوية من نفّذ.** مفيش `bash_history`. الواقعة اتبلّغت لـAnthropic.

⚠️ **الوقاية العملية الوحيدة:** خد نسخة 📦 من الأداة بعد كل جلسة. النسخة على جهازك هي المرجع اللي بره كل حاجة.

---

## 41) 🆕 مجموعات جديدة (9 أغسطس)

| المجموعة | إيه | القاعدة |
|---|---|---|
| **`roller`** | تعريف كل دور وصلاحياته | قراءة للمسجّلين · **كتابة للمالك بس** |
| **`tilgang`** | فهرس بالإيميل — **دي اللي القواعد بتقراها** | قراءة للمسجّلين · كتابة بالتسلسل |

⚠️ **`tilgang` لازم يبقى فيها `niva`** — القواعد بترفض أي مستند من غيره.

📌 **ولازم يتضافوا للست حاجات** (القسم 18-د): النسخ الاحتياطي · `TYPE_LABELS` · إلخ.

---

## 42) 🗓️ اللي حصل يوم 9 أغسطس

| # | |
|---|---|
| 1 | تعديل مجهول المصدر 06:46 — اتفحص واتبلّغ، والشغل اتعاد من الأول |
| 2 | نظام التسعير بنموذجين — اللوحة وفيبس بيدّوا نفس الرقم (اتختبر على 7 عيلات) |
| 3 | خصم الطفل التالت 15% — عشان العضوية تفضل أوفر دايمًا |
| 4 | الشهور على البرنامج |
| 5 | الاستمارة بقت تقرا كل أرقامها من القاعدة |
| 6 | **التكلفة من 79% لـ1.8%** من الحصة المجانية |
| 7 | الأدوار والصلاحيات بقت بيانات + تسلسل مفروض في القواعد |
| 8 | مفتاح طوارئ للمالك في القواعد |
| 9 | دورة مقفولة قفلت المالك بره — اتصلحت |

📌 **الدروس مجمّعة في القسم 48.**

---

## 43) 🩺 البيانات الصحية — تمويه بنجوم

**11 حقل** في قسم «🩺 Helse» بيظهروا **★★★★★** لمين ما لهوش صلاحية: الحساسية · الأمراض · الأدوية · صعوبات التعلم · PPT + حقول الوصف.

**الافتراضي:** المالك ومدير المدرسة بس. خانة `helse` في تبويب الأدوار.

📌 **نجوم مش إخفاء** — المعلم يعرف إن فيه معلومة ومش من صلاحيته، **فما يسألش ولي الأمر عن حاجة المدرسة عندها**.

⚠️ **والتصدير مقفول بنفس الطريقة** — الحقول مايتخارش في نافذة التصدير. من غير دي، التمويه كان هيبقى مسرحية: الشاشة نجوم والملف اللي بيتبعت بالإيميل فيه كل حاجة.

📌 **القايمة مش مكتوبة في الكود** — بتتقرا من `PD_SEKSJONER`. حقل صحي جديد بيتمويه تلقائيًا.

### 🔴 حد الحماية

**ده تمويه في الشاشة، مش في القاعدة.** القيمة بتوصل للمتصفح، واللي يفتح الكونسول يقراها. الحماية الحقيقية محتاجة إعادة كتابة استعلامات البوابة (القسم 16).

📌 وده بيضيّق فجوة نص الخصوصية (البند 2) — **بس ما بيقفلهاش**.

---

## 44) 📖 الكتالوج بقى بيتولّد من النظام

**كان مكتوب بالإيد، وبعد يوم شغل بقى بيوصف نظام مش موجود.**

**اللي بيتقرا لحظيًا:** التبويبات ومجموعاتها · الأدوار وصلاحياتها · الأسعار والشهور · الخصومات والمواد والوديعة · الحقول الصحية · مستقبلي الإشعارات · عدد حقول الطالب.

**اللي فضل مكتوب: 17 تفسير** — ليه 18% سقف، ليه نجوم، إن القواعد هي اللي بتفرض. **دي أسباب قرارات، مش حقائق عن النظام.**

📌 والكتالوج بقى **بيكشف** كمان: «⚠️ ما فيش مستقبلين» · «⚠️ أطفال foresatt 2 مش محسوبين» · «⚠️ سعر مش متسجّل».

⚠️ **اتفق مع النمط:** الوثيقة اللي بتتكتب بالإيد بتقدم مهما كنا منضبطين. **الحقائق تتولّد، التفسيرات تتكتب.**

---

## 45) 🧹 أدوات التنضيف

### أ) الأشخاص — Oversikt → Ansatte

| | |
|---|---|
| 🔴 نفس الإيميل مكرر | النظام بيقرا واحد عشوائي |
| ⚠️ نفس الاسم بإيميلات مختلفة | 4 إيميلات = 4 أشخاص |
| 📇 دخول من غير كارت | بيدخل ومش ظاهر في القوايم |
| 🔑 كارت بأدوار من غير دخول | العكس |
| 🔴 إيميلات مش صالحة | نص متلزق في خانة غلط |

📌 زرار **«Opprett kort»** بيفتح الشاشة والإيميل متعبّي — عشان ما تكتبوش بإيدك وتعمل شخص خامس.

⚠️ **والحذف بيشيل `tilgang` كمان** لو الشخص ما بقاش موجود — وإلا يفضل عنده صلاحية في القواعد.

### ب) كل القوايم — Oversikt → 🧹 Dubletter

**16 نوع، ومفتاح مختلف لكل واحد:**

```
الصفوف · البرامج    →  الاسم
الطلاب · الطلبات    →  الاسم + تاريخ الميلاد
الحصص              →  الصف + اليوم + الوقت
الحضور             →  الطالب + التاريخ
أولياء الأمور       →  الإيميل، والتليفون
```

🔴 **أهم حالة: إخوة بنفس الاسم.** لو المفتاح كان الاسم بس، النظام كان هيقول «تكرار» وتمسح طفل حقيقي. **عشان كده الطلاب بالاسم + تاريخ الميلاد.**

📌 **مفيش حذف تلقائي** — «افتح الاتنين الأول». واحد ممكن يكون فيه بيانات أكتر.

---

## 46) 🔴 تلات أعطال من نفس النوع في يوم واحد

**تصادم أسماء المتغيرات — والملف بقى 557 كيلو.**

| # | | النتيجة |
|---|---|---|
| 1 | `const lov` مرتين في `oppdaterOpprettKnapper` | **الموديول وقف كله** — الشارة حمرا للأبد |
| 2 | `normEpost` معرّفة مرتين | اتمسكت **قبل التسليم** |
| 3 | `colDisplay` محلية في 4 أماكن (امبارح) | التصدير احتاج دالة خاصة |

### العطل الأول — أخطر واحد

```
Uncaught ReferenceError: Cannot access 'lov' before initialization
```

`const` جوّه كتلة **بيحجب الخارجي في الكتلة كلها** — بما فيها السطر اللي قبله. الخطأ ده **وقف الموديول**، فمفيش مستمع اتربط، والشارة فضلت «Kobler til…».

📌 **وفحص الـsyntax ما مسكهوش** — صحيح نحويًا، بيظهر وقت التشغيل بس.

⚠️ **وde `feillogg` هو اللي كشفه.** من غيره كنا هنفضل نخمّن.

### الوقاية اللي اتضافت

الشارة بقت تقول بعد **12 ثانية**:

```
"Ingen svar fra databasen"        ← ولا مستمع رد
"Mangler 3: students, teachers…"  ← البعض رد
```

📌 «Kobler til…» كانت بتعني حاجتين مختلفين — «بطيء» و«ميت» — **ومكانش فيه طريقة تفرّق**.

**وفحص التصادمات بقى جزء من كل تسليم:**

```bash
grep -oE '^  (const|let|function) \w+' | sort | uniq -d
```

---

## 47) 🗓️ اللي حصل بعد الظهر — 9 أغسطس

| # | |
|---|---|
| 1 | البيانات الصحية بنجوم + التصدير مقفول |
| 2 | الكتالوج بقى بيتولّد من النظام |
| 3 | أداة تنضيف الأشخاص |
| 4 | أداة تكرارات لـ16 نوع |
| 5 | 🔴 `lov` وقّف الموديول — اتصلح |
| 6 | الشارة بقت تقول ناقص إيه |
| 7 | `normEpost` مكررة — اتمسكت قبل التسليم |

**والحالة الحالية:**

```
roller       ✓ 13 دور
tilgang      ⚠️ فيه تكرارات وإيميلات غلط — محتاجة تنضيف
teachers     ⚠️ مريم حمدان ناقصة · تولين مكررة
الشارة        ✓ خضرا
```

---

## 48) 📌 دروس اليوم

**١. التكلفة جزء من الصح.** كود شغال وبيستهلك 79% من الحصة **مش شغال**.

**٢. الأرقام المترابطة تتفحص مع بعض.** خصم الإخوة وسعر العضوية مش مستقلين — وكتابة جملة تسويقية غلط في استمارة تسجيل أسوأ من باگ.

**٣. «الافتراض هو المنع» له استثناء:** لما النظام محتاج نفسه عشان يشتغل. **منظومة الصلاحيات لازم يكون ليها نقطة انطلاق بره نفسها** — وde `erEier()` في القواعد.

**٤. الوثيقة المكتوبة بالإيد بتقدم.** الحقائق تتولّد، التفسيرات تتكتب.

**٥. الملف الكبير بيصادم أسماءه.** 557 كيلو، وتلات تصادمات في يوم. **الفحص لازم يبقى آلي.**

**٦. الحارس اللي بيمنع شغل صحيح بيتتجاهل.** الأختام الموروثة كانت بتمنع ملفاتي — الحل إني أشيل الختم، مش أضعّف الحارس.

---

## 49) 🗓️ اللي حصل ليل 9 → 10 أغسطس

يوم واحد اتغير فيه `admin.html` **13 مرة**، والقواعد مرتين، و`index2` و`functions/index.js` مرة لكل واحد.

| # | |
|---|---|
| 1 | رقم الحساب اتشال من الكود → `settings/faktura` + فحص MOD-11 + IBAN محسوب |
| 2 | أجر الساعة (200/150) اتشال من الكود → `settings/lonn`، والبوابة تقرا نفس المستند |
| 3 | مصاريف المواد بقت **على البرنامج** (مدينابارن 150 · الأساسي 450) |
| 4 | الملكية بقت بيانات — `settings/eierskap` + نودنوكل من الكونسول (Trinn 3) |
| 5 | إعادة تنظيم التبويبات: 10 مجموعات → 7، و«Personale» جديدة |
| 6 | شاشة Økonomi → Oversikt: شريط السنة + «Krever en avgjørelse» |
| 7 | تبويب «Alle personer» — كل الأشخاص من كل المصادر بلا مجلدات |
| 8 | تبويب «Pålogget nå» — من نبضة الجلسة الموجودة، صفر كتابات جديدة |
| 9 | `foreldreTilgang` — فهرس الأهالي بالإيميل |
| 10 | زرار التستداتا بقى يولّد **طلبات** بشكل الاستمارة، ومجلد 🧪 منفصل |
| 11 | 🔴 تلات أعطال حقيقية اتكشفت — القسم 50 |
| 12 | `index4.html` اتفلتر (**جاهز، لسه ما اتنشرش**) |

---

## 50) 🔴 تلات أعطال كشفتها محاكاة الطلبات

**المحاكاة اتعملت عشان تجرّب مسار القبول الحقيقي بدل ما تحقن طلاب في القاعدة. الأعطال التلاتة دي كانت هتفضل مخفية لو حقنّا مباشرة.**

### أ) الاستمارة بتبعت **أكواد** مش أسماء

`mapRegistrationProgram` بتستنى `madina_islamske_skole`، والمولّد كان بيكتب اسم البرنامج الكامل. النتيجة: طالب **من غير برنامج**، وسعر التدريس صفر — وفضلت مصاريف المواد بس.

📌 **الدرس:** أي بيانات تجريبية لازم تمر من نفس المدخل بتاع البيانات الحقيقية.

### ب) القبول ماكانش بيعيد حساب الإخوة

بيحسب الطفل الجديد بس. وعشان الخصم بيتحط على **البرنامج الأغلى**، أول ما يدخل طفل ببرنامج أغلى **الترتيب كله بيتقلب** — والإخوة المخزّنة مبالغهم بقت غلط.

```
اللوحة (لحظي):    11 725 kr
المخزّن (البوابة):  12 300 kr
```

والبوابة وفيبس بيقروا **المخزّن**. اتصلح: القبول بيعيد حساب العيلة كوحدة، والطفل اللي دفع مايتغيرش مبلغه.

### ج) 🔴 حقل `id` موروث — الأخطر

القبول بينسخ `{ ...reg }` على الطالب، و`reg` جواه حقل `id` بتاع **الطلب**. والقراءة كانت `{ id: d.id, ...d.data() }` — والـspread بيدوس على الـid.

**فكل طالب اتقبل من طلب، الـid بتاعه في الذاكرة هو id الطلب.** أي `updateDoc` أو `deleteDoc` عليه بيروح لمسار مش موجود → `not-found` **في صمت**.

📌 وده يفسّر ليه الطلاب ماكانوش بيتمسحوا من اللوحة واضطر المالك يمسحهم من فايرستور بإيده.

**اتصلح في تلات مستويات:** `delete studentRecord.id` · قلب الترتيب لـ`{ ...d.data(), id: d.id }` في **10 مواضع** · وتنضيف للموجودين.

⚠️ **نفس النمط لسه في `index1.html` و`index4.html`** — محتاج نفس الفحص.

---

## 51) 👑 الملكية كبيانات — Trinn 3

`settings/eierskap → { eiere: [...], gjenopprettingEpost, gjenopprettingUtloper }`

- الملاك بيتحرروا من **Personale → Eiere**
- **القايمة ماتفضاش** — القاعدة بترفض `size() < 1`
- **حقلا الاستعادة مايتلمسوش من اللوحة** — فحص إن القيمة ما اتغيرتش

### 🔴 طريق الاستعادة
صاحب مشروع Firebase يفتح الكونسول (بيعدّي فوق القواعد) ويكتب `gjenopprettingEpost` + `gjenopprettingUtloper`. الإيميل ده يبقى مالك لحد الميعاد → يدخل → يصلّح القايمة → **يمسح الحقلين**.

📌 **الميعاد مش تزويق.** حقل منسي من غير انتهاء = باب خلفي دايم. ولو الحقل ناقص، المقارنة بتبقى `request.time < request.time` = **مايفتحش حاجة**.

### ⚠️ فخ Firestore اتمسك هنا
القواعد بتجمع كل المطابقات بـ**«أو»**. `match /settings/eierskap { allow write: if false }` جنب `match /settings/{id} { allow write: if erAdmin() }` **مابيلغيهاش**. الاستثناء لازم يبقى **جوه** الشرط:
```
match /settings/{id} { allow write: if erAdmin() && id != 'eierskap'; }
```

---

## 52) 🧪 التستداتا — طلبات مش طلاب

زرار **System → Oversikt → Testdata** بيولّد **طلبات** بنفس حقول الاستمارة بالظبط، وتتقبل من مجلد **🧪 Testsøknader** المنفصل.

- الإخوة **بنفس رقم التليفون** — لأن القبول بيتعرف على العيلة بالتليفون
- `varselSendt: true` — عشان الدالة المجدولة ما تبعتش إيصالات لعناوين وهمية
- الحقول الصحية فيها قيم فعلًا — عشان تمويه النجوم يتجرب
- كل حاجة `erTestdata: true`، **والعلامة بتتوارث** لولي الأمر والطالب عند القبول

⚠️ **المجلد معزول تمامًا** — التجريبي مش بيظهر في «Nye» ولا «📋 Alle»، والعدّادات بتعدّ الحقيقي بس.

⚠️ **الحسابات في Firebase Auth بره القاعدة** — المسح مايوصلهاش.

---

## 53) 👨‍👩‍👧 فلترة بوابة الأهالي — جاهزة، لسه ما اتنشرتش

**المشكلة:** `index4.html` بيحمّل **20 مجموعة كاملة** ويفلتر في المتصفح — كل الطلاب بحقولهم الصحية، وكل ملاحظات المعلمين.

**اللي اتعمل في الملف الجاهز:**
```
students        → where(documentId(), 'in', barn)
studentNotes    → where('studentId', 'in', barn)
attendance      → where('studentId', 'in', barn)
registrations   → where('foresatt1_epost'/'foresatt2_epost', '==', إيميلي)
guardians       → المستمع الكامل اتشال، والدخول بقى استعلام بالإيميل
```

📌 **شكل الاستعلام مختار عشان يعدّي من القاعدة الجاية.** Firestore بيرفض أي استعلام مايقدرش يثبت إنه جوه القاعدة.

⚠️ **ولو الفهرس مالقاش الربط، البوابة بتقول بالنص** «مانلاقيش ربط بين حسابك وأبنائك» — مش بتفتح فاضية.

**المتبقي:** `homework` · `teachers` · `classPosts` لسه كاملين (محتوى صف مش بيانات طفل).

---

## 54) 📌 دروس ليلة 10 أغسطس

**١. المحاكاة تمر من نفس الباب.** حقن الطلاب مباشرة كان هيخفي تلات أعطال. المسار هو اللي بيتجرب، مش النتيجة.

**٢. مصدرين للسعر بيتفرقوا دايمًا.** اللوحة بتحسب لحظيًا والبوابة بتقرا المخزّن. المخزّن لازم يتحدّث كوحدة واحدة، مش صف صف.

**٣. `{ id: d.id, ...d.data() }` فخ.** الـspread بيدوس على المفتاح اللي قبله. **مسار المستند لازم يتحط آخر حاجة.**

**٤. `catch` بيعدّ ومابيقولش هو نفس الفشل الصامت.** «صفر اتعدلوا» كانت رسالة عمياء — الكتابات كلها كانت مرفوضة. الرسالة لازم تقول **الكود الخام** والمجموعة.

**٥. القواعد بتتجمع بـ«أو».** قاعدة أخص مابتلغيش أعم. الاستثناء جوه الشرط.

**٦. الفلترة في المتصفح عرض مش سبر.** والاستعلام المفلتر من غير قاعدة تقدر تثبته = تنضيف بس، مش قفل.

---

## 55) ⏭️ نقطة البداية الجاية

**بالترتيب المتفق عليه:**

1. **نظام رسايل قابلة للنسخ** — كل رسالة خطأ/نتيجة فيها كود مصدر (`[MSG:ADM-4821]`) واسم الدالة والمجموعة وكود الخطأ الخام. النطاق: `alert()` والتوستات الحمرا. **من غير** رسايل النجاح. `confirm()` مرحلة تانية لوحدها لأنها بتوقف الكود (~40 نداء في مسارات حذف وفلوس).
2. **نشر `index4.html`** + تجربة بحساب ولي أمر
3. **رقم الحساب البنكي**
4. فحص نمط `id` في `index1` و`index4`
5. توزيع الصفوف — الطلاب بيتقبلوا من غير صف
6. `studentPayments` مالهاش علامة تستداتا — دفعات لطلاب ممسوحين بتفضل

📌 **وحالة الملفات ليل 10 أغسطس:** `admin.html` عند المالك فيه إصلاح جدول العيلة على الموبايل — **لسه ما اتنشرش**. و`index4.html` المفلتر جاهز — **لسه ما اتنشرش**.
