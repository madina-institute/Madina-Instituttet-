# ملخص مشروع مدرسة مدينة — نقطة الاستكمال

**آخر تحديث: 6 أغسطس 2026**

الصق الملف ده في أول رسالة في محادثة جديدة مع Claude عشان يفهم السياق كامل.

---

## 1) نظرة عامة

- **المدرسة**: Madina Skole — مدرسة إسلامية في النرويج (قرآن/إسلام/عربي/أردو)
- **الدومين**: madinaskole.no
- **مشروع Firebase**: `madina-instituttet`
- **الريبو**: `madina-institute/Madina-Instituttet-` (فرع `main`، **عام**)
- **البنية**: ملفات HTML مستقلة (بدون framework) + Firestore + Cloud Functions + Firebase Hosting + Storage
- **المجلد المحلي**: `C:\Users\yasse\iCloudDrive\Disktop\madina-vipps`

⚠️ **أسماء الملفات في الريبو لازم تكون إنجليزي.** اسم عربي بيكسر خطوة checkout في GitHub Actions وبيوقف **كل** النشر (حصل فعلًا مع `ملخص-المشروع-للاستكمال.md`). المحتوى بالعربي جوه الملف عادي.

## 2) الملفات

| الملف | الوظيفة |
|---|---|
| `admin.html` | لوحة التحكم الكاملة (~7000 سطر) |
| `functions/index.js` | Cloud Functions: فيبس + إيميلات + النسخ الاحتياطي |
| `index.html` | الصفحة الرئيسية (3 لغات) — لا تلمس Firestore |
| `index1.html` | بوابة المعلمين |
| `index2.html` | استمارة التسجيل العامة — **الوحيدة اللي بتكتب بدون تسجيل دخول** |
| `index3.html` | البوابة الرئيسية — لا تلمس Firestore |
| `index4.html` | بوابة أولياء الأمور |
| `foreldreinnsyn.html` / `larerinnsyn.html` | بوابات استعلام |
| `timeplan.html` / `kunngjoring.html` | الجدول / الإعلانات — لا تلمس Firestore |
| `madina-brosjyre.html` | بروشور تفاعلي بـ3 لغات (للمشاركة والطباعة) — مش في قايمة الموقع |
| `publish-tool.html` | أداة النشر (ترفع على GitHub API، فيها خانة `customPath`) |
| `slettelogg.html` / `statistikk.html` / `personvern.html` | سجل حذف / إحصائيات / خصوصية |
| `firestore.rules` | **توثيق فقط** — القواعد الفعلية في Firebase Console |
| `.github/workflows/firebase-hosting.yml` | نشر تلقائي للموقع (~40 ثانية) |
| `.github/workflows/firebase-functions.yml` | نشر تلقائي للـFunctions (~2-5 دقائق) |

## 3) البنية التحتية والنشر ✅

الموقع على **Firebase Hosting**. **GitHub Pages اتقفل نهائيًا** (Unpublish) في 4 أغسطس.

- DNS: سجل A → `199.36.158.100` + TXT verification
- `firebase.json` فيه `no-cache` لملفات HTML — حل مشكلة الكاش المزمنة
- Secret اسمه `FIREBASE_TOKEN` في GitHub → Settings → Secrets and variables → Actions
- التوكن **سليم بعد تفعيل 2SV** (اتأكدنا في 6 أغسطس)

**⚠️ الـ workflow اتغيّر في 4 أغسطس:** `FirebaseExtended/action-hosting-deploy` شال دعم `firebaseToken` وبقى يطلب `firebaseServiceAccount`، فكان النشر بيفشل بعد 9 ثواني. الحل: استخدام `firebase-tools` مباشرة:
```yaml
- run: npm install -g firebase-tools
- run: firebase deploy --only hosting --project madina-instituttet --non-interactive
  env:
    FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
```

**نشر ملفات الـFunctions:** المسار لازم يكون `functions/index.js` بالظبط. لو اترفع في الجذر، الـ workflow مش هيشتغل و**مفيش أي رسالة خطأ**.

## 4) 🔐 حماية الحساب والبيانات (اكتملت 6 أغسطس)

| البند | الحالة |
|---|---|
| فوترة Google Cloud نشطة | ✅ (كانت معلّقة وعطّلت الخدمة — اتدفعت) |
| بطاقة دفع احتياطية | ✅ |
| التحقق بخطوتين (2SV) | ✅ (4 passkeys + جهاز + رقم) |
| نسخ احتياطي أسبوعي تلقائي | ✅ **ومجرّب** |
| مالك تاني للمشروع | ✅ |

**⚠️ درس مهم:** الخدمة اتعطّلت في 6 أغسطس لأن البطاقة اترفضت. لو الفوترة وقفت، **Cloud Functions بتتوقف** — يعني فيبس والإيميلات. البطاقة الاحتياطية بتمنع تكرار ده.

### النسخ الاحتياطي — كيف يعمل
دالة `ukentligSikkerhetskopi` بتشتغل **كل أحد 04:00 بتوقيت أوسلو**:
1. تقرا الـ43 مجموعة كلها
2. تكتبهم في ملف JSON واحد في Storage تحت `backups/`
3. تبعت إيميل لـ`post@madinaskole.no` فيه **اسم الملف** والإحصائيات
4. تمسح النسخ الأقدم من 60 يوم

ودالة `taSikkerhetskopiNa` للتشغيل اليدوي:
`https://us-central1-madina-instituttet.cloudfunctions.net/taSikkerhetskopiNa`

**📌 لا يوجد رابط تحميل في الإيميل — عن قصد.** الرابط الموقّع بيشتغل لأي حد يفتحه بدون تسجيل دخول، والملف فيه بيانات أطفال وأولياء أمور. التنزيل بيتم من Firebase Console → Storage → `backups/`، وده بيتطلب تسجيل دخول بـ2SV.

(ملاحظة تقنية: `getSignedUrl` كانت بتفشل أصلًا بخطأ `iam.serviceAccounts.signBlob denied` — واخترنا نشيلها بدل ما نفتح الصلاحية، لأن ده أأمن.)

**⚠️ أي مجموعة جديدة لازم تتضاف يدويًا في `BACKUP_COLLECTIONS` داخل `functions/index.js`** — وإلا مش هتتنسخ ومفيش حاجة هتنبّه.

**آخر نسخة مؤكدة:** 6 أغسطس 2026 — 784 مستند · 0.30 MB

## 5) نظام الإيميلات ✅

- **Brevo** (SMTP) مربوطة بإضافة Firebase "Trigger Email" (مجموعة `mail`)
- دومين madinaskole.no موثّق في Brevo (DKIM + DMARC مدموج مع سجل Zoho)
- كل إيميلات النظام من: `Madina Skole <post@madinaskole.no>`
- **Firebase Auth**: Email verification → `post@madinaskole.no` | Password reset → `noreply@madinaskole.no`
- ⏸️ **مؤجل**: استقبال ردود الإيميلات تلقائيًا (Inbound Parsing)

## 6) 🔒 الأمان — إصلاح 4-5 أغسطس

### المشكلة اللي كانت موجودة
حوالي **25 مجموعة** كان فيها `allow read: if true` — **أي حد على النت بدون تسجيل دخول** كان يقدر يقرا بيانات الطلاب، أولياء الأمور، المعلمين، الرسائل الخاصة، وملاحظات الطلاب. خطر GDPR حقيقي (بيانات أطفال في النرويج).

### الحل المطبّق
كل `read: if true` بقت `read: if request.auth != null`، ماعدا:
- `registrations` → `create: if true` (الاستمارة العامة)
- `formAnalytics` → `create: if true` (عداد الزيارات)

واتضاف `match /{document=**} { allow read, write: if false; }` في الآخر.

**القواعد الفعلية في Firebase Console → Firestore → Rules.** ملف `firestore.rules` في الريبو **توثيق فقط، مش بينشر نفسه.**

### 🐛 الباگ المتكرر: onSnapshot قبل تأكيد الدخول

**دي أكتر غلطة اتكررت في المشروع — 3 مرات.**

الـ Firestore SDK **لا يعيد المحاولة** لما القراءة تترفض. فأي `onSnapshot` بيتربط قبل ما `onAuthStateChanged` تأكّد المستخدم، بيموت **بشكل دائم** والقايمة تفضل فاضية للأبد.

**الملفات اللي اتصلحت:**
- `index1.html` · `index4.html` · `foreldreinnsyn.html` · `larerinnsyn.html` (4 أغسطس)
- `slettelogg.html` · `statistikk.html` (6 أغسطس — كانوا مكسورين ومحدش لاحظ)

**الحل:** دالة `watch()` بتأجّل كل مستمع لحد `startDataListeners()` اللي بتتنادى بعد تأكيد الدخول.

📌 **قاعدة دائمة: أي `onSnapshot` جديد لازم يمر على `watch()`.**

### إصلاحات مرافقة
1. **ترتيب الدخول اتعكس** في `index1`/`index4`: تسجيل الدخول الأول → `getDocs` للبحث → لو مش موجود `signOut` فورًا.
   **فايدة جانبية:** قبل كده صفحة الدخول كانت تقول "لا يوجد حساب بهذا الإيميل" قبل التحقق من الباسورد — أي حد كان يقدر يعرف مين مسجّل في المدرسة.
2. **تسجيل الخروج بقى بيعمل `location.reload()`** — بدونه المستمعين بيموتوا ومش بيرجعوا لو دخلت تاني في نفس التبويب.

## 7) 📅 البرامج والتقويم المدرسي (5 أغسطس)

**الهدف: كل حاجة تتحكم فيها الإدارة من اللوحة بدون كود.**

### مجموعة `programs` — تبويب "📅 Programmer"
كل برنامج: الاسم · أيام التدريس (اختيار متعدد) · السعر لكل فصل · مفعّل/لأ

البرامج الحالية:
- `Madina Islamske Skole — Integrert program (Lørdag)` → Lørdag → 2750
- `Madinabarn (Søndag)` → Søndag → 1500

⚠️ **الربط بالاسم** — الصفوف والطلاب بيشاوروا على البرنامج بالنص. تغيير الاسم بيكسر الربط.

### مجموعة `schoolCalendar` — تبويب "🗓️ Skolekalender"
كل سجل: الوصف · النوع · من تاريخ · إلى تاريخ · البرنامج (فاضي = الكل)

- **`Fri (ingen undervisning)`** يقفل يوم كان مفتوح
- **`Ekstra undervisningsdag`** يفتح يوم كان مقفول
- **اليوم الإضافي يغلب الإجازة** لو اتعارضوا (قرار يدوي واعي > قاعدة عامة)

### الأثر في بوابة المعلم
`getAllowedWeekdays()` بقت تقرا من `programs` بدل مطابقة نص `'Lørdag'`. حقل تسجيل الساعات (`myAttDato`) اتحوّل للتقويم المخصص. أيام الإجازة بتبان باهتة بنقطة ذهبية واسم الإجازة عند اللمس.

📌 **الاحتياطي دايمًا بيضيّق مش بيوسّع** — لو البرنامج مفقود أو أيامه فاضية، بيرجع للسبت/الأحد مش لكل الأيام.

## 8) 💰 الأسعار الخاصة (5 أغسطس)

**كارت المعلم:** `Egen timesats (kr/t)` + `Begrunnelse`
- فاضي = الافتراضي (200 معلم / 150 مساعد)
- **"متطوع" يغلب على كل حاجة** — حتى لو فيه رقم في الخانة، الراتب صفر. حاجز مقصود.
- بيبان `egen sats` جنب الاسم في الجدول والتصدير

**كارت الطالب:** `Spesialpris (kr)` + `Begrunnelse`
- فاضي = السعر العادي مع خصم الإخوة
- **محمي من الحساب التلقائي** — كان `belop` بيتحسب تلقائي وبيمسح أي تعديل يدوي بصمت

## 9) الميزات المبنية في admin.html

1. **الكاشير**: المستحق/المدفوع/المتبقي، سجل دفعات، دفعة يدوية، رابور، تصدير CSV
2. **حساب المستحق تلقائيًا** + خصم إخوة (20% للتاني، 30% للثالث+)
3. **Kasserer (Ledelse)** — لدور "Eier" فقط: استرجاع فيبس، إلغاء طلب، خصم مخصص، فاتورة
4. **حذف هرمي**: "Eier" فقط يحذف حسابات الموظفين
5. **تصميم الكروت** + طباعة مخصصة
6. **الربط المتبادل**: أي اسم مرتبط قابل للضغط
7. **تحضير الدروس**: نموذج 60 دقيقة (شوطين) + حقول لكل مادة
8. **المجلدات**: الطلبات / الطلاب
9. **رصيد المدرسة** بفلتر شهري
10. **نظام الرواتب** الكامل
11. **جلسة دائمة للمالك**: `PERSISTENT_SESSION_EMAILS = ['yasser.m.s.abdelrahim@gmail.com']`

⚠️ **المستمعون في `admin.html` مكتوبين يدويًا** في قايمة `attachListener(...)` حوالي سطر 7061 — **مش بيتولّدوا من `SCHEMAS`**. أي مجموعة جديدة لازم تتضاف في المكانين، وإلا التبويب هيظهر فاضي بدون أي خطأ (حصل مع `programs`).

## 10) 🏠 بوابة المعلم — تحسينات 5-6 أغسطس

**المعلمون بيستخدموا الموبايل أساسًا** — ده غيّر أولويات التصميم.

### شاشة "Min side" (نقطة البداية بعد الدخول)
1. ترحيب حسب الوقت + التاريخ (نرويجي/عربي)
2. **من الإدارة** (فوق — بطلب المستخدم)
3. **Oppgaver som venter behandling**: ساعات غير مسجّلة (يومين + زرار "إظهار المزيد") · رسايل غياب · تحضير ناقص · رسايل جديدة
4. **Mine timer** — حصص المعلم في أقرب يوم دراسة من مجموعة `hours`

⚠️ `hours` مكانتش محمّلة في البوابة أصلًا — المعلم عمره ما شاف حصصه. اتضافت.

### كارت "Mine timer" في شاشة الساعات
الشهر (قابل للتغيير) · الساعات المعتمدة · المنتظرة · أجر الساعة · المبلغ

📌 **المبلغ من الساعات المعتمدة فقط** — الرقم ميوعدش بحاجة الإدارة ما وافقتش عليها.

### تحسينات الموبايل
- **35 رسالة `alert()` اتحوّلت لـ toast** — نوافذ النظام كانت بتوقف الصفحة
- toast بقى بلونين: أحمر للأخطاء (5 ثوانٍ) وأخضر للتأكيدات (2.6)
- حماية عامة للجداول والصور من التمدد الأفقي

⚠️ **تحذير من تجربة:** التعديل الآلي على `width:...px` لمس **media queries** وحوّلها لصياغة غلط كانت هتكسر تخطيط الشاشات الكبيرة. أي تعديل جماعي على CSS لازم يستثني `@media`.

## 11) 🗑️ سجل الحذف (6 أغسطس)

**`slettelogg` بيحفظ نسخة كاملة من السجل المحذوف (`data`)** — مش مجرد سجل بأسماء. فهو فعليًا بيغطي حالة "حد مسح بالغلط".

**اللي اتضاف/رجع:**
- **قفل 30 يوم** على `Bekreft sletting` مع عدّاد الأيام (متحقّق منه في الدالة كمان مش في الشكل بس)
- **`Gjenopprett` مفتوح دايمًا** — الاسترجاع آمن، اللي محتاج يبقى بطيء هو رمي النسخة
- **استرجاع زمني**: آخر ساعة / يوم / أسبوع
- **تجميع بالنوع** + "استرجاع الكل"
- الاسترجاع الجماعي بيقول "وقفت بعد 7 من 12" لو فشل في النص

⚠️ **`slettelogg` بيحمي من الحذف اليدوي — مش من ضياع قاعدة البيانات**، لأنه قاعد جواها. عشان كده النسخ الاحتياطي في Storage مهم بالتوازي.

📌 **`deletionLog` فيها 443 سجل** — أكبر مجموعة في القاعدة، أكبر من كل بيانات المدرسة. تستاهل تنضيف السجلات القديمة من مرحلة التجارب.

## 12) 🐛 أخطاء تانية اتصلحت (مرجعية)

**أ) الكاشير كان بيعرض 0** — `onSnapshot` قبل الدخول (نفس الباگ المتكرر).

**ب) إيميلات إعداد الحساب مكانتش توصل للجدد** — الكود كان بينشئ الحساب في جلسة مؤقتة ويبعت الإيميل من نفس الجلسة وبعدين يقفلها فورًا → الطلب بيتقطع. الحل: قفل الجلسة المؤقتة → إرسال من الجلسة الأساسية.

**ج) شارة "آخر نشر" كانت مضللة** — كانت بتقرا من GitHub Pages. بقت تقرا آخر تشغيل ناجح لـ`firebase-hosting.yml`.
⚠️ **حدود**: مبتقيسش النشر من `firebase deploy` المباشر، ولا نشر الـFunctions.

**د) البروشور كان بيتمدد أفقيًا على الموبايل** — كان مقاس A4 ثابت (210mm). الحل: إعادة بناء كصفحة متجاوبة بدون مقاسات ثابتة.

## 13) ⏳ المتبقي قبل الإطلاق الحقيقي

1. `VIPPS_ENV=production` في `functions/.env.madina-instituttet`
2. مفاتيح فيبس الحقيقية في **Firebase Secret Manager** — الكود بيستخدم `defineSecret` بالفعل. أي سر جديد يحتاج deploy تاني
3. رقم التاجر الحقيقي (`VIPPS_MSN=465281` رقم اختبار)
4. **⚠️ منطقة الدوال `us-central1`** — كل الـCloud Functions شغالة في أمريكا. البيانات بتتنقل هناك وقت المعالجة. لمدرسة نرويجية الأنظف `europe-west1`. مش عاجل، لكن يستاهل قبل الإطلاق.
5. **المرحلة 2 من الأمان (لسه ما اتعملتش)**: أي حساب مسجّل يقدر تقنيًا يقرا/يكتب أي حاجة — بما فيها `salaryPayments` و`teacherAttendance` (معلم يقدر يوافق على ساعات نفسه).

   ⚠️ **ملاحظة معمارية**: التقييد حسب الدور **مش شغل قواعد بس** — البوابات بتحمّل المجموعات كاملة وتفلتر في المتصفح (`index1` بيحمّل 25 مجموعة، `index4` بيحمّل 20). و Firestore بيرفض الاستعلام كله لو فيه ولا مستند واحد ممكن يتعارض. فقاعدة "ولي الأمر يشوف ابنه بس" هتخلي البوابة تطلع **فاضية** مش تفلتر. يعني ده محتاج إعادة كتابة الاستعلامات في الكود.

6. **المرحلة 3 (مؤجلة)**: ربط المعلم بأكتر من صف/برنامج. حاليًا `tildeltKlasse` قيمة واحدة، فالمعلم اللي بيدرّس اليومين مالوش تمثيل. **دي بتلمس تسجيل الدخول والصلاحيات — أخطر واحدة.**

7. **تنضيف `deletionLog`** (443 سجل)

## 14) ملف الإعدادات الحالي

`functions/.env.madina-instituttet`:
```
VIPPS_ENV=test
VIPPS_MSN=465281
SITE_BASE_URL=https://madinaskole.no
```

📌 **قاعدة**: أي قيمة فيها SECRET/KEY/PASSWORD **متتحطش هنا أبدًا** (الريبو عام) — تروح لـSecret Manager.

## 15) ملاحظات تقنية دائمة

1. **أي كود JS جديد لازم يتفحص syntax قبل التسليم** (`node --check` على محتوى `<script type="module">`)
2. **أي `onSnapshot` جديد لازم يمر على `watch()`** — الغلطة دي اتكررت 3 مرات
3. **أي مجموعة جديدة تحتاج 4 حاجات**: قاعدة في `firestore.rules` + `attachListener()` في `admin.html` + `cache` + إضافتها في `BACKUP_COLLECTIONS`
4. **أسماء الملفات في الريبو إنجليزي دايمًا**
5. **مسار ملفات الـFunctions لازم يبدأ بـ`functions/`**
6. الروتين الآمن قبل أي نشر من الكمبيوتر: `git pull` → التعديل → `firebase deploy`
7. **مصدر الحقيقة الوحيد هو GitHub.** أي ملف على الجهاز أو في محادثة تانية أقدم من آخر رفعة = نسخة قديمة. حصل أكتر من مرة إن نسخة قديمة اترفعت فوق تعديلات أحدث ومسحتها.
8. **رسايل الـcommit كلها `Oppdater X via publiseringsverktøy`** — بتقول أنهي ملف اتغير بس مش إيه اللي اتغير. ده ضيّع وقت في تتبع تعديل قديم.

## 16) ⏸️ شغل جاهز لكن مش مرفوع

- **`publish-tool.html` محسّنة** (6 أغسطس): `functions/index.js` في القايمة الجاهزة · تحذير لو المسار غلط · تنبيه إن نشر الـFunctions أبطأ · **خانة وصف للتعديل** تظهر في تاريخ GitHub
